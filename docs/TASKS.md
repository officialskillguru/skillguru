# SkillGuru — Task Tracker

_A persisted snapshot of engineering work in flight, so anyone opening the repo cold can see what's done, in progress, or pending without re-deriving it from chat history. Update this file whenever task status changes — treat it as a lightweight, human-readable companion to whatever live task list is being used in a given session._

## Phase 0 — Project Stabilization

| # | Task | Status |
|---|---|---|
| 0.1 | Regenerate Supabase database types, remove orphaned type files | ✅ Done |
| 0.2 | Consolidate duplicate Supabase client factories onto `lib/supabase/client.ts` | ✅ Done |
| 0.3 | Remove verified-dead shadow code (dead auth hooks, route guards, `AuditService`, `useAdminLeads`) | ✅ Done |
| 0.4 | Fix public `leads` Edge Function schema mismatch (BUG-01) — deploy + live-test | ✅ Done |
| 0.5 | Fix all TypeScript errors and ESLint errors | ✅ Done (0 tsc errors, 0 eslint errors) |
| 0.6 | Fix critical DB security findings (`SECURITY DEFINER` view, unrestricted policies, anon-callable functions, search_path) | ✅ Done (leaked-password-protection remains a manual Auth dashboard step) |
| 0.7 | RLS performance cleanup migration (consolidate overlapping permissive policies, wrap `auth.uid()`) | ✅ Done |
| 0.8 | Repository hygiene cleanup (stale scripts/snapshots, `.gitignore`) | ✅ Done |
| 0.9 | Update documentation after Phase 0 fixes | ✅ Done (this pass) |

**Phase 0 status: complete**, except one manual step (leaked-password-protection toggle in the Supabase Auth dashboard — not reachable via available tooling).

## Phase 1 — Admin → Mentor → Course Management (in progress)

| # | Task | Status |
|---|---|---|
| 1.1 | Admin creates mentor account end-to-end (`create-mentor` Edge Function fixed — removed writes to nonexistent `profiles.role`/`profiles.status`) | ✅ Done |
| 1.2 | Deliver credentials to new mentor — shown in Admin UI modal (explicit decision: no email provider yet) | ✅ Done |
| 1.3 | Mentor login + forced password change on first login (`user_settings.password_reset_required`, guard redirects, `ForcePasswordChangePage`) | ✅ Done |
| 1.4 | Course Management completeness — see detail below | ✅ Done |
| 1.5 | Student enrollment flow | ✅ Done |
| 1.6 | PaymentService + MockPaymentProvider architecture | ✅ Done |
| 1.7 | Certificates | ✅ Done |
| 1.8 | Notifications completeness | ✅ Done |
| 1.9 | Analytics completeness | ✅ Done |
| 1.10 | Production QA pass | ✅ Done — see `QA_REPORT.md` |
| 1.11 | Razorpay integration | ⏳ Pending |

**Phase 1.4 detail (Course Management)** — done this pass:
- Fixed `has_permission()` (used nonexistent `p.code` instead of `p.slug`) — this was silently blocking **all** course/module/lesson/resource writes system-wide, including for admins.
- Seeded missing `courses.create`/`courses.update_own`/`courses.update_all` permission rows and granted them to mentor/admin roles.
- Created 5 storage buckets (`courses`, `mentors`, `success-stories`, `admins`, `students`) with folder-based RLS; rewrote `storage.service.ts` to register uploads in the `files` table with rollback-on-failure.
- Built `curriculum.service.ts` — full CRUD + reordering for modules/lessons/resources (previously did not exist anywhere in the codebase).
- Rewrote `CourseBuilderTab` in `MentorDashboardPage.tsx` — real module/lesson CRUD, reorder (up/down), video/PDF upload wired to `storage.service.ts`, and Publish/Unpublish wired to `updateCourseStatus`. Replaces the previous 100%-hardcoded stub.
- Fixed the student-facing "Mark Complete" button (`CourseLearningPage.tsx`): it was calling a stub `learningService.markLessonComplete` that wrote **hardcoded fake numbers** (`completed_lessons: 1, total_lessons: 10, completion_percentage: 10`) to `course_progress` regardless of actual progress. Switched it to the correct, already-existing `progressService.markLessonComplete` (via `useProgress()`), which writes real per-lesson rows to `lesson_progress`; confirmed the `recalculate_course_progress()` trigger (already installed, safe-by-design) keeps `course_progress.completion_percentage` in sync automatically. Removed the dead stub method and its now-unused repository plumbing.
- Fixed mentor `StudentsTab` progress column (previously hardcoded `0%`) — `getMentorStudentsList()` now joins `course_progress` for real completion percentages.

**Phase 1.5 detail (Student enrollment flow)** — done this pass:
- Discovered `courses.price` was silently dropped by the domain-model mapper (`mapCourseRowToDomain`) even though the column exists and is populated — every course showed a hardcoded `"Free"` label on `CourseDetailsPage`, and the real Razorpay path bypassed the repository entirely to read `price` directly. Added `price` to the `Course` domain model and mapper; removed a stale, incorrect comment in `course.persistence.ts` claiming "price... is not in the courses table."
- Fixed `useCheckEnrollment.ts` (previously unused, and broken: queried `enrollments.user_id`/`.id` — neither exists; real columns are `student_id`/`course_id`). Now correctly detects whether a student already owns a course, and `CourseDetailsPage` uses it to show "Continue Learning" instead of an enroll button for owned courses.
- **HIGH-risk finding, live-verified**: `enrollments` has no student-facing INSERT policy at all — only `admin`. Confirmed via RLS simulation (`set local role authenticated`) that a real student's direct insert is rejected (`42501`). Per your explicit choice, resolved via a new Edge Function (`enroll-free`, deployed, `verify_jwt: true`) rather than an RLS change — it validates the course is published and free server-side (so a crafted request can't self-grant a paid course), checks for an existing enrollment (idempotent), then inserts using the service-role key. `enrollments` RLS is untouched.
- Wired `CourseDetailsPage`'s "Enroll Now"/"Enroll Free" button: free courses call the new `enroll-free` function via `enrollInFreeCourse()`/`useFreeEnroll()`; paid courses go through the `PaymentService`/`create-order`/`verify-payment` path built in Phase 1.6 (see below), which resolved the previously-broken BUG-02 path.

**Phase 1.6 detail (PaymentService + MockPaymentProvider)** — done this pass, per `docs/PAYMENT_ARCHITECTURE.md` (25-section architecture doc written and approved before any code was touched):
- Built a `PaymentProvider` interface (`createOrder`/`verifyPayment`/`refund`) inside the Edge Functions, with `MockPaymentProvider` and `RazorpayProvider` implementations behind it, resolved once server-side via a `PAYMENT_PROVIDER` env var (`_provider.ts`, mirrored across all 5 payment functions). The frontend never imports a provider — it only calls the Edge Functions by HTTP.
- Rewrote and deployed all 5 payment Edge Functions against the real schema (`create-order`, `verify-payment`, `refund-payment`, `payment-status`, `payment-webhook`) — previously **zero** of these were deployed; all existed only as schema-broken draft source (BUG-02/03/05).
- Fixed several schema mismatches found while rewriting: `invoices` doesn't have `user_id`/`amount`/`status` columns (only `order_id`/`invoice_number`/`pdf_url`); `notifications`' real shape is `recipient_id`/`title`/`message`/`type`/`is_read` with category living in `metadata`, not the `category`/`status` columns the old draft assumed; `payment-status`/`refund-payment` called a nonexistent `current_user_roles` RPC (real one: `get_current_roles`).
- Mock mode has no real checkout widget, so `create-order` hands back a server-generated, correctly-HMAC-signed "simulated completed checkout" pair (`mockPayment`) that the frontend immediately verifies against — this exercises the exact same signature-verification code path Razorpay mode uses, never bypassing it.
- Rewrote the frontend `payment.service.ts` (renamed export `razorpayService` → `paymentService`) and `usePayment.ts` to call the new functions by their real names, and **removed the insecure client-side dev-fallback entirely** (the root cause of BUG-02's ₹0-enrollment security hole) — updated all 4 call sites (`CourseDetailsPage`, `AdminPaymentsPage`, `AdminAnalyticsPage`).
- Live-verified: all 4 JWT-gated functions correctly reject unauthenticated calls; `payment-webhook`'s HMAC verification accepts a correctly-signed test payload and correctly logs+no-ops on an unmatched order (confirmed via `webhooks` table read, then cleaned up the test row).
- **Not yet exercised**: a full authenticated end-to-end "browse → pay → enrolled" click-through (no test student credentials available in this environment, same limitation noted in Phase 1.5) — recommend one manual pass before considering this fully production-verified.

**Phase 1.7 detail (Certificates)** — done this pass:
- **Migration applied (approved before running)**: `issue_certificate_for_completed_enrollment()` trigger function + `AFTER UPDATE OF status` trigger on `enrollments` — auto-issues a `certificates` row (unique `certificate_number`/`verification_code`, linked to the existing "Default Certificate" `certificate_templates` row) the moment an enrollment's status flips to `completed`. Previously **nothing** inserted into `certificates` anywhere in the codebase (confirmed live: 0 rows). Live-verified end-to-end with a real insert→update→check→cleanup cycle (test data removed after).
- Fixed `certificates.repository.ts`'s `getStudentCertificates()` — was a hardcoded stub (`fail(new UnexpectedError("Feature stubbed", "drift"))`) that made the student "My Certificates" page always error out. Now does a real join through `enrollments` to `courses`.
- Fixed a real bug in `MyCertificatesPage.tsx`: the "Download" button used `cert.certificate_file_id` (a `files` table UUID FK, not a URL) directly as an `<a href>` — clicking it would navigate to a broken `/uuid` path. No PDF-generation pipeline exists yet (a real LaTeX/PDF service is a distinct, larger future feature — see Vision 2026 in `DEVELOPMENT_ROADMAP.md`), so this pass ships a **fully working**, non-placeholder alternative instead: a new `CertificateViewPage` (`/dashboard/certificates/:id`) that renders the certificate's real `certificate_templates.html_template` (already built for exactly this — `{{studentName}}`/`{{courseName}}`/`{{issueDate}}`/`{{certificateNumber}}` placeholders) in an iframe, with a working "Print / Save as PDF" button via the browser's native print dialog.
- Built a public `VerifyCertificatePage` (`/verify-certificate/:code`, no auth required — matches `certificates`' intentionally-public `SELECT` RLS) wiring up the previously-unused `useCertificateVerification` hook, so anyone (e.g. an employer) can confirm a certificate is authentic by its verification code.
- Added an admin "Backfill Missing" action on `AdminCertificatesPage` — issues certificates for any `completed` enrollment that predates the trigger (direct insert; admins already have unconditional `certificates` INSERT RLS).

**Phase 1.8 detail (Notifications completeness)** — done this pass:
- Found and removed another shadow-pair (same pattern as Phase 0's BUG-09/10/11): `notification.service.ts` (singular) and `notifications.repository.ts` were **100% hardcoded stubs** — the repository even extended `BaseRepository<"courses">` (the wrong table entirely, though harmless in practice since every method was hardcoded to return static empty/ok values without ever querying). These were the *only* things the student-facing `useNotifications` hook called — meaning every student's notification bell, mark-read, mark-all-read, and delete were silent no-ops that never touched the database. Deleted both dead files (zero other callers) and rewired the hook onto the real, already-working `notifications.service.ts` (used correctly by the admin side).
- The student dashboard's notification bell icon was a literal disabled placeholder button (`opacity-50 cursor-not-allowed`, `title="Coming in a future release"`, no `onClick`) — replaced with a real `NotificationBell` component: live unread-count badge, dropdown list, mark-read-on-click (navigating to `action_url` when present), "mark all read," and a realtime subscription so new notifications appear without a refresh.
- Mentor Dashboard's Notifications tab (flagged as a static "All Caught Up" placeholder since Phase 0/BUG-17) is now wired to the same real service/hook — real list, real mark-read, real mark-all-read.
- **Migration applied (approved before running)**: extended the certificate-issuance trigger from Phase 1.7 to also insert a real notification ("Certificate issued!", with a working `action_url` to the certificate view page) the moment a certificate is auto-issued. Live-verified end-to-end (insert→complete→check both certificate and notification rows→cleanup).
- Added focus-visible styles to the new notification list items and "mark all read" buttons (interactive elements that previously would have relied on hover-only affordance).
- **Known follow-ups, not built this pass**: `deleteNotification()` exists in the service/hook but `notifications` has no DELETE RLS policy — not wired into any UI (nothing calls it, so no broken button exists, but it's a dead end if someone reaches for it later). `notification_preferences` (email/in-app/category toggles) has a real service method but no settings UI anywhere.

**Phase 1.9 detail (Analytics completeness)** — done this pass:
- Confirmed `AdminAnalyticsPage.tsx` (revenue/enrollment/student-growth/CRM-funnel charts) was **already fully real** — well-built, honest empty states, no fabrication found there. Fixed the one remaining fake figure feeding into it: `analyticsService.getTopCourses()`'s `revenue: enrollments * 5000` approximation, replaced with a real sum of `order_items.price` for completed orders per course. Also fixed that same query undercounting total enrollments (it filtered to `status = 'active'` only, silently excluding every student who'd since completed the course).
- Fixed `dashboard.service.ts` (BUG-08): `getDashboardChartData()` was injecting `Math.random()` into a "placement rate" figure on every call and hardcoding ₹15,000 mock revenue per enrollment; `getDashboardMetrics()` hardcoded `totalLeads: 0` despite a real, populated `leads` table. Removed the fabricated `placementRate` field entirely (confirmed zero real consumers — there's no placement-tracking schema to honestly derive it from, consistent with the project's existing "Feature Not Available" convention for Placements rather than faking it) and replaced mock revenue with a real sum of `payments.amount` grouped by month. Extended `getDashboardMetrics()` with real `totalOrders`/`totalRevenue`/`certificatesIssued`.
- **The bigger finding**: all 9 Admin Dashboard widgets (`DashboardStats`, `PlatformOverviewWidget`, `SystemHealthWidget`, `RecentMentorsWidget`, `RecentOrdersWidget`, `RoleManagementWidget`, `TopCoursesWidget`, `RecentActivitiesWidget` — `QuickActionsWidget` excluded, its navigation was already real/correct) were rendering **100% hardcoded demo data** (BUG-07) — fake student counts, fake ₹14,82,000 revenue, fake mentor names, fake orders, uniformly-green fake system health, `Math.random()`-flavored placement stats. Meanwhile a real, already-correct `useDashboardData()` hook existed (wired to the now-fixed `dashboard.service.ts`) but had **zero consumers anywhere** — the dashboard simply never used it. Rewired all 8 widgets to real data: `useDashboardData()`, `useMentors()`, `analyticsService.getTopCourses()`, and direct real queries for recent orders / role-user counts, following the same patterns already proven in `AdminPaymentsPage`/`AdminCertificatesPage`.
- `SystemHealthWidget` redesigned as a lightweight real check (DB reachability, live storage bucket count, current payment provider) linking to the already-real, already-good `AdminSystemHealthPage` (`/admin/health`) for the full report — also fixed that page's stale "Edge Functions: Issue, deploy supabase functions" status, which predated this session's 8 function deployments.

## Phase 2 — Admin

Not yet started as a dedicated phase. Known open items (see BUG_REPORT.md / FEATURE_STATUS.md):
- [x] Admin Dashboard: replace hardcoded widget data with real queries (BUG-07/08) — done as part of Phase 1.9.
- [ ] Fix mentor status toggle overwriting bio instead of changing status (BUG-06).
- [ ] Rewrite `successStories.service.ts`, `admins.service.ts`, `cms.service.ts` against the real (existing) tables (BUG-12/13).
- [ ] Fix `AdminRolePage`'s fake audit history (BUG-14).
- [ ] Fix course-creation flow's random-mentor assignment (BUG-15).

## Phase 3 — Mentor

- [x] Mentor Dashboard Course Builder is now functional (see Phase 1.4 above).
- [x] Mentor Dashboard student-progress column now shows real completion percentages.
- [x] Mentor Dashboard Notifications tab now wired to the real notifications service (see Phase 1.8 above).

## Phase 4 — Student

- [x] Course Learning page "Mark Complete" now writes real per-lesson progress (see Phase 1.4 above).
- [ ] Course Learning page video player is a static placeholder ("Video Player (Mock)") — real video is now uploadable/stored via `storage.service.ts`, but the player itself still needs to consume `getSignedFileUrl()` for `lessons.video_file_id`.
- [ ] Several student pages (Chat, Support, Assignments) marked "Unclear" in FEATURE_STATUS.md — need a dedicated audit pass before scheduling work.

## Phase 5 — Payment

Not yet started. Architecture direction agreed (see DEVELOPMENT_ROADMAP.md "Payment System" note): `PaymentService` facade + `MockPaymentProvider`/`RazorpayProvider` behind a common interface, provider selection via env var only. This will supersede the current insecure client-side "dev fallback" (BUG-02) rather than patching it.
- [ ] Design `PaymentService` interface (`createOrder`/`verifyPayment`/`capturePayment`/`refund`).
- [ ] Implement `MockPaymentProvider` (simulate order creation, success, failure, cancellation, verification, refunds).
- [ ] Implement `RazorpayProvider` stub with `TODO` markers for `key_id`/`key_secret`/`webhook_secret`.
- [ ] Deploy the real payment Edge Functions and fix their schema references (BUG-02/03).
- [ ] Remove the insecure client-side dev-fallback path.
- [ ] Implement real Razorpay refunds (BUG-05).

## Phase 6 — Notifications

Core notifications system is functional (real-time, mark-read, preferences — see FEATURE_STATUS.md). No dedicated phase work currently planned.

## Phase 7 — Testing

Not yet started. Known gap: only 2 of ~37 service files have any test coverage (TECHNICAL_DEBT.md § 9). Priority order once started: payments, CRM (the two domains with the most historical bugs).
- [ ] Add a CI pipeline (GitHub Actions) running `tsc --noEmit`, `eslint .`, `vitest run` on every PR.
- [ ] Grow service-layer test coverage, prioritizing payments and CRM.

## Phase 8 — Deployment

Not yet started as a dedicated phase.

---

## Phase 1.12 — Design System + Admin Dashboard redesign

Per an explicit "UI/UX Constitution" direction: the app should be judged on Linear/Stripe/Notion/Apple-tier UX, not typical admin-template quality. This is a large, multi-page effort — per your explicit sequencing, it proceeds **design system first, then Admin Dashboard, then every other page migrates to that same system** (not redesigned independently).

- [x] Audited the existing primitive layer (`src/components/ui/`) — already a solid shadcn/ui + Radix foundation (Button, Badge, Card, Table, Dialog, Sheet, Dropdown, Command palette, Tabs, Accordion, Toast, Skeleton, Empty/Error states, Progress, Pagination all already existed). Documented the full inventory in `docs/DESIGN_SYSTEM.md` rather than rebuilding what already worked.
- [x] Fixed a real gap: **dark mode had zero CSS** — the toggle existed and flipped a `.dark` class, but no dark-theme color values were ever defined, so it silently did nothing. Added a full token set.
- [x] Added `sidebar`/`sidebar-foreground`/`sidebar-muted`/`sidebar-border`/`sidebar-accent` tokens (the app shell stays dark in both themes, Linear/Vercel-style) — replaces hardcoded `#0F172A`/`slate-800` literals in the Admin sidebar.
- [x] Enhanced `DataTable` (already had sort/filter/search/pagination/column-visibility/bulk-actions/row-selection): added built-in CSV export and sticky-header support.
- [x] Added missing badge variants (`destructive`, `info`, `muted`).
- [x] Redesigned the Admin shell (`AdminPage.tsx`) — see BUG-28 for the full list of fake elements found and fixed (fake notification count, fake avatar, non-functional collapse button, dead-code breadcrumbs, broken dark mode).
- [x] Migrated `DashboardHeader`/`DashboardStats`/`PlatformOverviewWidget`/`SystemHealthWidget` to the token system with hover elevation and refined typography.
- [ ] **Not done this pass, explicitly deferred per your sequencing**: the 5 table-based Dashboard widgets still carry default Tailwind table styling (functionally real since Phase 1.9, visually not yet elevated); every other admin page (CRM, Courses, Payments, etc.) is next in the migration queue.
- [ ] Live browser verification not done — no admin test credentials available in this environment (same limitation as `QA_REPORT.md`).

## Phase 1.13 — Users (Students) and Mentors admin pages

Migrated `AdminStudentsPage`/`AdminMentorsPage` to the `DataTable`/token system — but this surfaced functional problems far bigger than styling.

- [x] **`AdminStudentsPage` was 100% fabricated** — a hardcoded fictional student roster, a fake "Approve & Generate PG Certificate" button that only mutated local state, a fake "Magic Link" toast with no feature behind it. Rebuilt entirely against real data: `useStudents()` (now scoped to the `student` role — it previously returned every profile including admins/mentors), a real per-student enrollment/progress/certificate summary, and a drawer linking to the real Phase 1.7 certificate view. See BUG-29.
- [x] **`AdminMentorsPage`** — real core flow (list/create/edit mentor) was already solid, but had several fakes: hardcoded "5.0" rating for every mentor (now real, computed from approved `testimonials`), a fake "Assign Course" menu action, and completely non-functional bulk activate/suspend/delete (called a no-op stub, `useBulkUpdateMentorStatus`, now deleted — zero other callers). Wired "Force Password Reset" for real using the same mechanism from Phase 1.3. See BUG-30.
- [ ] **Still open**: real mentor active/suspended status (BUG-06) needs a schema decision before bulk activate/suspend can be rebuilt for real — not invented ad hoc during this pass.

## Phase 1.14 — CRM and Courses admin pages

Audited `AdminCRMPage`/`AdminCoursesPage` for the same fake/hardcoded-data pattern found in Phase 1.13. CRM turned out to already be substantially real; Courses had the worst fakery found since `AdminStudentsPage` (BUG-29).

- [x] **`AdminCRMPage`** — audited and confirmed already genuinely real end-to-end (`crm.service.ts`-backed leads/pipeline/tasks/metrics, already on design tokens). Migrated the Leads tab's hand-rolled `<table>` onto the shared `DataTable` component (built-in CSV export, consistent toolbar) — a styling/consistency fix, not a data-correctness one. See BUG-32.
- [x] **`AdminCoursesPage`'s course editor drawer had 6 fake tabs** — Duration and Price inputs were permanently inert (`value={0} onChange={() => {}}`) despite both being real, actively-used columns; the Curriculum tab showed 3 hardcoded fake module names with a no-op "Add Module" toast despite a real curriculum CRUD system already existing in the Mentor Course Builder; the Mentors tab showed 4 hardcoded fake names as disabled checkboxes implying multi-select, when `courses.mentor_id` is actually a single required FK; the Media tab's uploader was a fake toast; the SEO tab was fully decorative with no schema backing; and the course list table's Enrollments column was a hardcoded `0` for every course. See BUG-31.
- [x] Extracted the real curriculum editor (`CourseCurriculumEditor`/`ModuleEditor`/`LessonRow`/`LessonForm`, previously private to `MentorDashboardPage.tsx`) into a shared `src/components/shared/CourseCurriculumEditor.tsx` component, so the Admin course editor and the Mentor Course Builder now run the exact same real implementation instead of the admin side faking a parallel one.
- [x] Wired Duration/Price to the real columns, replaced the fake Mentors checkbox list with a real single-mentor-select (`useMentors()`), replaced the fake Media uploader with a real `storage.service.ts` upload writing `thumbnail_file_id`, honestly disabled the SEO tab (no schema support, same treatment as the existing Discount Subtitle field), and added a real per-course enrollment count query.
- [ ] **Still open**: `AdminCoursesPage`'s course list table itself (not the drawer) still uses raw `bg-white`/`text-slate-*` styling and a hand-rolled `<table>` rather than `DataTable` — deferred in favor of fixing the fake-data bugs first; next in the migration queue.

## Phase 1.15 — Payments and Certificates admin pages

Audited `AdminPaymentsPage`/`AdminCertificatesPage` for the same fake-data pattern found in Phase 1.13/1.14. Unlike Courses, both turned out to already be fully real.

- [x] **`AdminPaymentsPage`** — confirmed `getAdminPayments`/`getRevenueMetrics`/`initiateRefund` all hit real tables and the real `refund-payment` Edge Function, no stubs found. Migrated the hand-rolled `<table>` onto the shared `DataTable` component (built-in CSV export, replacing the manual export button).
- [x] **`AdminCertificatesPage`** — confirmed `fetchCertificates`/`backfillMissingCertificates` are real inline queries against `certificates`/`enrollments`/`certificate_templates`, no stubs found. Migrated onto `DataTable`. Fixed a real (if minor) gap: the "Verification" column showed a static "Issued" badge for every row instead of the certificate's actual verification code — now links to the real public `/verify-certificate/:code` page.
- [x] See BUG-33 for the combined writeup.

## Phase 1.16 — Notifications and Settings admin pages

Audited `AdminNotificationsPage`/`AdminSettingsPage`. Notifications was minor (a real feature with a missing control); Settings was the worst finding since `AdminStudentsPage` (BUG-29) — a false "this persists" claim sitting directly next to a save button that discarded everything.

- [x] **`AdminNotificationsPage`** — already real and on tokens (`notifications.service.ts`-backed). Fixed a real gap: pagination `page` state fed the query but no Prev/Next controls existed in the UI, capping every admin at the first 30 notifications regardless of how many existed. Added real controls using `results.length === pageSize` as the has-more signal (the service returns a plain array, no total count).
- [x] **`AdminSettingsPage`'s Platform Parameters tab** — `cms.service.ts` was a full no-op stub (`getSetting` always `null`, `setSetting` a no-op) with a comment falsely claiming `system_settings` doesn't exist. Rewrote the service for real against the actual key-value table. The page's "System Status" card had been asserting "edits here will persist" the entire time this was false — that claim is now actually true instead of aspirational. `AdminCMSPage.tsx` shares the same hooks, so it's fixed too, automatically.
- [x] **`AdminSettingsPage`'s Audit tab** — was backed by a hardcoded-`[]` stub (`useAuditLogs` in `useAdminSystem.ts`) rendering a differently-shaped fake record type that didn't even match the real `audit_logs` schema — while a fully real, working audit log page (`AdminAuditLogsPage.tsx`) already existed at `/admin/audit-logs`. Found and deleted a *second*, independent stub of the same hook in `useAdminData.ts` (zero consumers) plus its backing `auditLogs.service.ts` file entirely. Replaced the fake tab with an honest link to the real page rather than duplicating its query a third time. Corrected a stale BUG-10 entry in `BUG_REPORT.md` that had incorrectly claimed this was already fixed.
- [x] Wired the decorative API-secret "eye" icon (no `onClick` at all) to a real show/hide toggle.
- [x] See BUG-34 for the combined writeup.

## Phase 1.17 — Admin module completion (Courses DataTable, Role/Operators, System Health, full fake-sweep)

Per direction to finish the remaining Admin migration and remove every remaining fake implementation in the Admin module before starting the Mentor module.

- [x] **`AdminCoursesPage`'s course list table** migrated from a hand-rolled `<table>` (manual `selectedIds`/`toggleSelection`/`toggleAll` state) onto `DataTable`, with a real checkbox-selection column wired to `bulkActions` (Publish/Archive/Delete) and built-in CSV export. Header/stats/filter cards migrated to design tokens.
- [x] **`AdminRolePage`** — all three tabs were fake (BUG-14 plus two more found alongside it): the "Permissions Matrix" toggled local state with a misleading success toast and no persistence; "Operations Users" was a hardcoded 4-person array; "System Audit Logs" was `mockAuditLogs`. Rebuilt: Permissions Matrix is now a real read-only view of `role_permissions`/`permissions` (editable enforcement isn't built yet, so it's honestly read-only rather than fake-interactive); Operators queries real admin-role profiles (which required fixing `admins.service.ts`'s `listAdmins` stub — also fixes part of BUG-13); Audit Logs uses a new shared `auditLogs.service.ts` extracted from `AdminAuditLogsPage.tsx` so both pages run one real implementation instead of a third duplicate.
- [x] **`AdminSystemHealthPage`** — the "External Services" panel hardcoded `ok: true` for all 5 rows regardless of actual status. Added real checks for Supabase Auth (`getSession()`), Storage (`listBuckets()`), and Realtime (a bounded-timeout channel-subscribe probe). Payment Provider/Edge Functions rows — which this page has no way to actually ping — now show honest neutral `info` badges instead of an unverified "Healthy" claim.
- [x] **Full fake-implementation sweep of the remaining Admin module**: dispatched a research pass across the 11 previously-unaudited admin pages (Placements, AI Guidance, Success Stories, Coupons, Refunds, Chat, Support, Calendar, Assignments, Invitations, CMS). Confirmed real with no action needed: Coupons, Refunds (minor "record-only, doesn't call Razorpay" note), Chat, Support, Calendar, Assignments. Fixed: Success Stories' fully-stubbed service (BUG-35), AI Guidance's 100%-fabricated dashboard (BUG-36), CMS's fake Blogs tab (BUG-37), and — found independently while verifying the invite Edge Function was actually deployed — the mentor invite/resend flow calling a nonexistent Edge Function (BUG-38, previously known but unfixed).
- [x] Corrected a stale claim in `BUG_REPORT.md`'s BUG-10 entry that a previous pass had already flagged as wrong once before.

**Admin module status**: every remaining admin page has now been read end-to-end for fake/hardcoded/decorative-only UI this session. Verdicts: real & migrated to tokens where touched (most pages), or honestly labeled "Feature Not Available" where no real data source exists yet (Placements, AI Guidance, CMS Blogs) — none left presenting fabricated data as live. `AdminCoursesPage`'s editor drawer's SEO tab and Pricing's Discount Subtitle field remain honestly disabled (documented BUG-31/pre-existing), and `admins.service.ts`'s write methods (create/update/disable/delete admin accounts) remain intentionally unimplemented stubs — no UI calls them since `AdminRolePage`'s Operators tab is now read-only, so nothing is misrepresenting them as working.

## Phase 1.18 — Mentor module (started)

Per direction: after the Admin module reached production quality, begin the Mentor module against the same design system. `MentorDashboardPage.tsx` was already largely on design tokens (built in earlier phases), so this pass focused on fake/dead-interaction removal rather than a token migration.

- [x] Audited `MentorDashboardPage.tsx` (Overview/Courses/Course Builder/Students/Analytics/Notifications/Profile tabs). Fixed 3 fakes: hardcoded `analyticsViews: 1042` metric (see BUG-39), a dead "Go to Classroom" button, and a non-functional Profile tab. See BUG-39 for the full writeup.
- [x] Added real `getMentorProfile`/`updateMentorProfile` to `mentor-portal.service.ts` and corresponding `useMentorProfile`/`useUpdateMentorProfile` hooks — the Profile tab is now genuinely read/write against `mentor_profiles`.
- [ ] **Not yet audited this pass**: `MentorsPage.tsx` (public mentor listing) and `MentorProfilePage.tsx` (public mentor profile) — next in the Mentor-module queue. The Course Builder tab's curriculum editor was already fixed/shared with Admin in Phase 1.14 (`CourseCurriculumEditor`).

_Last updated: 2026-07-24, Phase 1.10 (Production QA pass) + Phase 1.12 (Design System + Admin Dashboard) + Phase 1.13 (Users/Mentors pages) + Phase 1.14 (CRM/Courses pages) + Phase 1.15 (Payments/Certificates pages) + Phase 1.16 (Notifications/Settings pages) + Phase 1.17 (Admin module completion) + Phase 1.18 (Mentor module, started) complete — see `QA_REPORT.md` and `DESIGN_SYSTEM.md` for full findings._

## Phase 2.1 — AI Voice Sales Agent: Database + RAG foundation

Full plan and architecture: see `docs/AI_VOICE_AGENT.md`. Requested as a full enterprise voice-AI platform (RAG, n8n, ElevenLabs, Gemini, Twilio, payments, calendar, analytics) in one shot; scoped down to real, verified increments instead of building disconnected scaffolding against mostly-blank credentials (no n8n instance, no payment/calendar/Twilio-number config yet).

- [x] Flagged a real security issue first: live-looking API keys/secrets (Gemini, ElevenLabs, Twilio, Gmail app password) were pasted directly into chat — none written to any file; user advised to rotate them.
- [x] Surveyed the codebase before building anything: confirmed no existing chat/voice AI UI, no n8n integration, no vector/embeddings setup — nothing to duplicate. Confirmed the correct reuse points: `crm.service.ts` (leads/pipeline/tasks), `notifications.service.ts`, the existing `meetings` table (for future appointments), and the existing `PaymentProvider` abstraction (for future payment wiring).
- [x] Enabled `pgvector`; created `agent_conversations`, `agent_messages`, `agent_knowledge_documents`, `agent_knowledge_chunks` (with `vector(768)` + HNSW cosine index), `agent_customer_memory`, `agent_logs`; extended the existing `leads` table with qualification columns (`city`/`country`/`education`/`experience_years`/`career_goal`/`timeline`/`learning_mode`/`urgency`/`lead_score`/`agent_conversation_id`) rather than creating a duplicate leads table.
- [x] Added `match_agent_knowledge()` — a real, callable semantic-search RPC over the corpus.
- [x] Applied RLS to every new table, then immediately consolidated the policies (single policy per action, `(select ...)`-wrapped) after the performance advisor flagged the same per-row-re-evaluation issue this project already fixed once project-wide in Phase 0.7 — not left as new debt.
- [x] Regenerated `database.types.ts` from the live schema. Full verification suite green: `tsc --noEmit`, `eslint .` (0 errors), `vitest run` (18/18), `npm run build`.
- [x] **Phase 2.2 — Knowledge ingestion (2026-07-26)**: built and deployed `sync-knowledge-base` Edge Function (real Gemini `text-embedding-004` calls, per-document error isolation, idempotent upsert via a new `(source_table, source_id)` UNIQUE constraint added specifically to avoid the check-then-write race the `create-mentor` incident already exposed). Built the real service/hook layer (`knowledge-base.service.ts`/`useKnowledgeBase.ts`) and a new admin page (`AdminKnowledgeBasePage.tsx`, `/admin/knowledge-base`) to trigger sync, view corpus stats, and toggle documents active/inactive. Verified current real data is thin (2 test-fixture courses, 3 mentors, 0 approved testimonials) — the pipeline is real and correct regardless of how much content exists yet to feed it.
- [x] **Phase 2.3 — Production AI conversation engine (2026-07-26)**: built and deployed the `converse` Edge Function — the channel-independent reasoning layer (AIProvider abstraction, `GeminiProvider`, prompt manager, 14-state conversation state machine, 16-intent/16-entity extraction, deterministic lead scoring, a 4-tool tool-calling framework wired to real `match_agent_knowledge`/`leads`/`support_tickets`, DB-backed rate limiting, PII-safe logging). Full detail in `docs/AI_VOICE_AGENT.md`. Verification suite green: `deno check` clean (both `_shared/` and `converse/`), `tsc --noEmit` 0 errors, `eslint .` 0 errors, `vitest run` 63/63 (45 new tests), `npm run build` succeeds. Deployed to `thhivnrxthsvoxftblwg`, live-smoke-tested (clean typed response, imports resolve, Supabase env vars work).
- [x] **`GEMINI_API_KEY` set and live-tested (2026-07-26)**: found and fixed two Google-side model deprecations along the way (`gemini-2.5-flash` → `gemini-flash-latest`; `text-embedding-004` → `gemini-embedding-001` with `outputDimensionality: 768`), redeployed `converse` and `sync-knowledge-base`. Ran real conversation turns against the live function (greeting, course inquiry, pricing, prompt injection) — intent detection, entity extraction, state transitions, lead scoring, tool execution, and DB-persisted logging all confirmed real via direct queries. The anti-hallucination safeguard correctly refused to guess course/pricing details against the (currently empty) knowledge base rather than fabricating an answer.
- [x] **Knowledge base synced and RAG verified end-to-end (2026-07-26)**: user triggered a real sync from `/admin/knowledge-base`. Independently verified (not just trusting the UI): 3 real documents + 3 real 768-dim embedded chunks in the DB; `match_agent_knowledge()` RPC tested directly via SQL (self-query similarity 1.0, related-doc similarity 0.74 — correct ranking); a real `converse` call retrieved the actual chunks, correctly set `groundedInKnowledge: true`, returned a response whose claims exactly matched the retrieved content, and persisted real citations to `agent_messages`. Full chain confirmed: documents → chunks → RPC → tool call → grounded response → citations → logging.
- [ ] **Blocked before Phase 2.4**: this Gemini key is on Google's free tier (5 requests/minute) — unusable for production traffic; needs a paid-tier upgrade or key swap. Corpus is still thin (test-fixture course/mentor content only) — real answers need real course/mentor/testimonial data synced. Remaining scenario coverage (mentor info, FAQ, unknown question, full lead-qualification/escalation/multi-turn flows) deferred until quota allows.

## Phase 1.19 — Mentor Profile full real rebuild (2026-07-27)

Public `MentorProfilePage` looked personalized/DB-backed but ~90% of its content was fabricated by `mentorProfileFactory.ts` (hardcoded education, one hardcoded AWS certification, two hardcoded fake projects, invented work-experience/awards, and fake reviews/testimonials attributed to invented named students). The "Book Session" flow was a pure `console.log` + `setTimeout` mock — nothing ever persisted. Full real rebuild requested and delivered end-to-end. See `BUG-41` for the complete writeup.

- [x] **Migration** (`20260727000001_mentor_profile_content.sql` + `20260727000002_mentor_profile_content_rls_cleanup.sql`): 4 new normalized tables (`mentor_experience`, `mentor_projects`, `mentor_certifications`, `mentor_achievements`) — each with RLS (public read / owner write / admin manage, consolidated to one policy per command after the advisor flagged the same `auth_rls_initplan`/`multiple_permissive_policies` pattern this project already did a dedicated Phase 0.7 cleanup for), indexes (`mentor_id, sort_order` + covering indexes on every FK), soft delete, and `created_by`/`updated_by` audit columns matching the existing `mentor_profiles`/`courses` convention. `meetings` gained a real `attendee_id` column (previously only host/admin could see or touch any booking) plus scoped attendee SELECT/cancel-only-UPDATE policies. New `get_mentor_available_slots()` SECURITY DEFINER RPC (locked to `authenticated` only — the advisor initially flagged it as `anon`-callable since Postgres grants EXECUTE to PUBLIC by default; fixed by explicit REVOKE, matching the Phase 0.6 precedent for `calculate_profile_completion`/`get_unread_notification_count`) expands a mentor's weekly `availability` template into concrete free slots, excluding conflicts with existing non-cancelled meetings. Live-verified: RPC slot generation, conflict exclusion, and RLS (positive: attendee sees/cancels own booking; negative: attendee's attempt to set an arbitrary status is rejected by `WITH CHECK`) — all via insert→verify→rollback cycles, zero residual data.
- [x] **`book-mentor-session` Edge Function** (deployed, `verify_jwt: true`) — mirrors the `enroll-free` pattern exactly: re-validates the requested slot is still free server-side (re-running the same availability+conflict logic, not trusting the client), inserts the real `meetings` row via the service-role key (since `meetings` intentionally has no student-facing INSERT policy), and sends a real notification to the mentor via the existing `notifications` table. No fake success path exists anywhere in this flow.
- [x] **Backend rebuild**: new `mentor-profile-content.service.ts` (CRUD + reorder for all 4 content types, following `curriculum.service.ts`'s exact conventions), `mentor-availability.service.ts`, `mentor-booking.service.ts` (replaces the old mock `bookSession`). Rewrote `mentor.repository.ts` — fixed a real pre-existing bug (`designation`/`total_students`/`rating` columns read by the old code don't exist on `mentor_profiles`; real columns are `headline`/`experience_years`, no rating column), removed 100% of the fabricated content, and wired every section to real data: experience/projects/certifications/achievements (new tables), reviews (real `testimonials` joined through `courses.mentor_id` — no salary/company-outcome fields exist there, so those were dropped rather than invented), skills (real `mentor_profiles.skills`, rendered as plain tags — the fabricated 0-100 proficiency score is gone), courses taught (real published courses with real enrollment counts and real average ratings), companiesWorkedWith (derived from real experience entries), availability (real `availability` table + the new RPC). Deleted `mentorProfileFactory.ts`, the dead `mockMentorData.ts` (zero importers), and the dead `mentor.mapper.ts` (zero importers) entirely. `RelatedMentors` rewired from a static `data/platform.ts` lookup to a real `mentor_profiles` expertise-overlap query.
- [x] **Scope decisions** (architect calls, not further fabrication): dropped the `education` field entirely (zero UI consumers found — building schema for a field nothing renders would be waste); kept `mentorshipProcess`/FAQ as static generic platform copy (not attributed to a specific person, so not part of the fabrication problem); removed the fake skill-proficiency bars.
- [x] **Mentor Dashboard** — new `src/components/mentor/MentorProfileSections.tsx`, wired into `MentorDashboardPage.tsx`'s Profile tab as sub-tabs (Basic Info/Experience/Projects/Certifications/Achievements/Availability/Sessions). Every section is genuine add/edit/delete(+reorder for the 4 content types) CRUD, with real image upload (`storage.service.ts`'s `mentors` bucket) for project images and certification issuer logos, real weekly-availability management, and a real upcoming-sessions list with cancel.
- [x] **Bugs fixed in passing while touching these files**: `MentorAchievements.tsx` silently dropped any achievement with an unrecognized icon (now falls back to a default icon instead of disappearing); `MentorCourses.tsx`'s "View Details" link used a dead `#/courses/:slug` hash path instead of the real `/courses/:slug` route; `MentorCard.tsx` rendered `<img src="">`  when no avatar exists (now falls back to initials, matching the BUG-28 pattern); `MentorHero.tsx`'s "Book Counselling" and "Share Profile" buttons had no `onClick` at all (now wired to the real booking modal and a real share/copy action respectively).
- [x] Full verification suite green after every milestone: `tsc --noEmit` (0 errors), `eslint .` (0 errors / 64 pre-existing warnings, none introduced), `vitest run` (63/63), `npm run build` (succeeds).

## Phase 1.20 — Mentor module continuation: real video playback + real mentor account lifecycle (2026-07-27)

Continuing the Phase 1 "complete mentor module" directive. Audited the remaining open items from `FEATURE_STATUS.md` before building anything, to avoid rebuilding what was already real.

- [x] **Audit pass**: confirmed Chat, Support, Assignments, and the student-facing Mentors listing are all genuinely real (real Supabase reads/writes, real Realtime subscription for chat, no fake-success paths) — reclassified from "Unclear" in `FEATURE_STATUS.md`. No rework needed.
- [x] **BUG-42/43 — real video/PDF playback**: found and fixed a storage-RLS folder-path bug (lesson uploads were foldered by `moduleId`, but the `courses` bucket's RLS policy checks enrollment against the folder as if it were the `courseId` — silently blocking every real student from ever reading their own lesson videos). Then replaced `CourseLearningPage.tsx`'s `<p>Video Player (Mock)</p>` with a real `<video>`/embedded-PDF viewer (new `resolveFileUrl()` helper in `storage.service.ts`, unifying public/signed URL resolution) plus a real lesson-resources list with working open/download.
- [x] **BUG-06 — real mentor account lifecycle**: migration `20260727000003_mentor_status_lifecycle.sql` adds real `status`/`deleted_at` to `mentor_profiles` (additive, RLS tightened to hide soft-deleted mentors publicly). Rewrote `mentors.service.ts` (real suspend/reactivate, soft-delete, restore — removed the old irreversible hard delete and the bio-clobbering status stub), deleted the fully-dead duplicate `mentors.repository.ts`. Wired real Suspend/Delete/Restore controls into `AdminMentorsPage.tsx` (row actions + Danger Zone), with status/deleted filters. **Real enforcement**: `MentorProtectedRoute` now checks the mentor's live status and blocks a suspended/deleted mentor from the Mentor Dashboard with a genuine "Account Suspended/Removed" screen, not just a cosmetic admin-side badge.
- [x] Live-verified: soft-deleted mentor confirmed invisible to a simulated non-admin RLS session; both fixes covered by the full verification suite (`tsc`/`eslint`/`vitest`/`build`, all green, zero new errors).
- [x] **Mentor "Reply to reviews"** (2026-07-27): migration `20260727000004_mentor_reply_to_reviews.sql` adds `mentor_reply`/`mentor_replied_at` to `testimonials` plus a `reply_to_testimonial()` SECURITY DEFINER RPC (verifies course ownership server-side via the existing `is_course_mentor()` helper; only ever writes the reply fields, never the student's own rating/content — avoids needing column-level GRANT surgery on a role shared with the existing "Admins manage testimonials" policy). New `testimonials.service.ts` (real `listMentorTestimonials`/`replyToTestimonial`, no duplicate of the existing read-only rating-aggregation code in `mentor-portal.service.ts`/`AdminMentorsPage.tsx`), a new "Reviews" tab in the Mentor Dashboard with real reply/edit-reply UI, and the reply now displays on the public `MentorProfilePage` under each review. Live-verified both directions: a course's real mentor can reply (confirmed via RLS-simulated session), and a mentor who doesn't own the course is correctly rejected by the RPC's own authorization check (`P0001: Not authorized to reply to this review`) — both cycles rolled back, zero residual data.
- [ ] **Not yet done** (remaining Phase 1 scope per the roadmap): a dedicated mentor analytics deep-dive beyond the existing real per-course stats (enrollments/completion-rate/avg-rating).

**Mentor module: considered feature-complete for this pass** — moving to the Student module per the priority order.

## Phase 2.0 — Student module continuation (2026-07-27)

Per the priority order (Student module next). Audited before building: Chat/Support/Assignments/Mentors listing already confirmed real (Phase 1.20). Found and fixed two further genuine gaps:

- [x] **BUG-44 — "Continue Learning" always resumed at lesson 1**: `continue-learning.service.ts`'s own comment admitted it was a stub ("just return the first lesson always for now"). Rewrote to use the real `lesson_progress` table (already used elsewhere, e.g. `CourseLearningPage.tsx`) to resume at the actual first-incomplete lesson, with real mentor name and real module/lesson completion counts (previously hardcoded to `"SkillGuru Faculty"` / `0`/`0`) wired into `DashboardPage.tsx`.
- [x] **BUG-45 — Wishlist had a real backend, zero UI**: `commerce.service.ts`'s wishlist methods were fully real and RLS-correct but had no consumer anywhere except a disabled "(Coming Soon)" button. Built `useWishlist.ts` hooks, wired the real toggle button on `CourseDetailsPage.tsx`, and added a new `/dashboard/wishlist` page + real sidebar nav entry.
- [x] **BUG-46 — fabricated gamification sidebar**: `DashboardSidebar.tsx` hardcoded "Level 12"/"80%"/"24 MB Storage"/"Daily Goal: Done" with no backing schema at all, shown on every dashboard page. Replaced with real enrolled-course/certificate counts. Building a real XP/leveling system was out of scope (needs product decisions on point values/thresholds) — removing the fabrication was the correct scope, not inventing a new system.
- [x] Full verification suite green throughout (`tsc`/`eslint`/`vitest`/`build`); live RLS verification for the wishlist add/remove cycle (rolled back, zero residual data).
- [x] **BUG-47 — Quiz/mock-test system**: zero UI on either side (mentor authoring, student taking) despite a real schema and an already-offered `"quiz"` lesson content-type. Found and fixed a broken lesson linkage (`quizzes.module_id` instead of a real per-lesson column), a real enrollment-id bug in the existing broken `useQuiz.ts`, and — most seriously — an RLS gap letting any authenticated user read a published quiz's answer key before attempting it. Migration adds `quizzes.lesson_id`, tightens the three SELECT policies to require real enrollment, and adds a `submit_quiz_attempt()` server-side scoring RPC. Built real mentor authoring (`quiz-authoring.service.ts`, wired into `CourseCurriculumEditor.tsx`'s "Manage Quiz" panel, MCQ/true-false only - no fabricated auto-grader for multi_select/short_answer) and real student-side quiz-taking (`QuizSection` in `CourseLearningPage.tsx`). Live-verified: correct scoring, the leak closed, enrollment-impersonation rejected - one further bug (a nonexistent `completed_at` column referenced in the RPC's first draft) caught and fixed by this same live-verification process before landing.
- [x] **BUG-48 — Student Profile avatar upload**: uploaded to a nonexistent `avatars` storage bucket (every real upload failed), displayed via a broken URL construction (file ID used as storage path), and a profile save showed a false "success" toast even on failure. Fixed to reuse the canonical `storage.service.ts` upload pipeline (`students` bucket, already correctly RLS-scoped) and `resolveFileUrl()`.
- [x] **BUG-49 — Learning Analytics**: weekly study-hours chart was a permanent placeholder despite real `lesson_progress.time_spent_seconds` data already existing. Built a real aggregation and made the previously-decorative range dropdown genuinely functional.
- [x] **Notes — new feature, previously entirely unbuilt**: no table, service, or UI existed for a student's own lesson notes anywhere in the codebase (`student_notes`/`mentor_notes` are an unrelated CRM concept — staff notes about a student). Added a new `lesson_notes` table (owner-scoped RLS, one note per student/lesson, soft-delete), a per-lesson "My Notes" panel directly on `CourseLearningPage.tsx` (add/edit/save inline), and a dedicated `/dashboard/notes` page listing every note across all courses with delete. Real sidebar nav entry added (was previously absent, not disabled). Live-verified: an owner can read/write their own notes; a different authenticated student sees zero rows and cannot forge an insert under another student's `student_id`.
- [x] **Placement Module — enterprise rebuild (2026-07-27)**: was entirely unbuilt (no `hiring_partners`/`job_postings`/etc. schema anywhere) — both `AdminPlacementsPage` and the student sidebar's disabled "Placement" tab already honestly disclosed this rather than faking data, so this was greenfield build, not a bug fix. Per explicit direction, built a full enterprise Placement Management System rather than a minimal job board:
  - **Schema** (`20260727073017_placement_module_schema.sql` + `..._revoke_anon.sql`): 9 new tables — `hiring_partners`, `job_postings`, `placement_applications`, `application_documents`, `interview_rounds`, `interview_feedback`, `placement_offers`, `placement_status_history` (insert-only audit trail), `saved_placements`. Reused existing infrastructure everywhere possible instead of duplicating it: interview scheduling reuses `meetings` (added no new columns — `entity_type`/`entity_id`/`attendee_id` already existed from the mentor-booking feature), student notifications reuse `notifications`, resumes/documents/offer letters reuse `storage.service.ts`'s `students` bucket and the `files` registry, and `student_profiles.resume_file_id` is the default resume. `public.organizations` was considered and explicitly rejected for hiring partners — it models platform tenants, not companies hiring students. Every privileged pipeline transition (`apply_to_job`, `withdraw_application`, `advance_application_stage`, `schedule_interview_round`, `record_interview_feedback`, `release_offer`, `mark_placement_joined`) is a SECURITY DEFINER RPC — `placement_applications` and its dependent tables have no direct INSERT/UPDATE policy at all, matching the `submit_quiz_attempt`/`reply_to_testimonial` pattern. `hiring_partners.partner_user_id` (nullable, unused) reserves future hiring-partner login without needing a later migration.
  - **Backend**: `placement-partners.service.ts`, `placement-jobs.service.ts`, `placement-applications.service.ts`, `placement-interviews.service.ts`, `placement-offers.service.ts`, `placement-documents.service.ts`, `saved-placements.service.ts` + `hooks/student/usePlacements.ts` and `hooks/admin/useAdminPlacements.ts`.
  - **Admin UI** (`AdminPlacementsPage.tsx`, full rebuild): Hiring Partners CRUD, Job Postings CRUD, and a full Applications pipeline view (per-stage counts, applicant drawer with shortlist/reject/schedule-interview/record-feedback/release-offer/mark-joined actions, and a real status-history audit trail).
  - **Student UI** (`pages/student/PlacementsPage.tsx`, new): Browse/search/filter jobs, save jobs, apply with a real resume upload (`uploadFile("students", ...)` then `apply_to_job` RPC — no fabricated success path), track application status with full timeline, withdraw, view upcoming interviews (reusing the same real `meetings` row a mentor-booking student would see), and offer/placement history. Sidebar's "Placement" nav entry flipped from `disabled: true` to a real route (`/dashboard/placement`).
  - Live-verified the entire pipeline end-to-end in one rolled-back transaction: apply → duplicate-application rejected → shortlist → schedule interview → record feedback → select → release offer → mark joined, confirming the owning student sees every row (application, interview round, meeting, offer, 6-entry status history) while a different student and a non-admin are rejected/see nothing at every step.
- [x] **Resume Builder — new feature (2026-07-27)**: previously entirely unbuilt (no table/service/UI anywhere). Reused existing data instead of duplicating it — contact/links/summary come from `profiles` (giving `bio` and the link columns their first-ever edit UI), education/skills reuse `student_profiles` — and added only the genuinely list-shaped, currently-absent sections as new tables: `resume_experience`, `resume_projects`, `resume_certifications`, `resume_achievements` (owner-scoped RLS, soft delete, mirroring `mentor_experience`/etc.'s exact conventions). PDF export uses the browser's native print-to-PDF (`window.print()` + a scoped `@media print` rule) rather than a new client-side PDF-rendering dependency — zero bundle-size cost, and the output is genuine selectable text, not a screenshot. New `/dashboard/resume-builder` page (Edit + Preview tabs), real sidebar nav entry. **Live verification caught a real pre-existing gap**: `student_profiles` (like `mentor_profiles`) has an admin-only INSERT policy by deliberate project convention, so a student could never create their own row directly — fixed with a new `upsert_my_student_profile()` SECURITY DEFINER RPC, hardcoded to `auth.uid()` (no student-id parameter, so no impersonation surface), matching the established privileged-write pattern. Live-verified: a student can self-create their `student_profiles` row via the RPC and manage their own resume sections; a different student sees zero rows everywhere.
- [x] **AI Career Guidance — new feature (2026-07-27)**: previously entirely unbuilt for students (the public `GuidancePage.tsx` is an unrelated marketing lead-capture page). Reuses the AI Voice Agent's `AIProvider`/`GeminiProvider` abstraction and the same `GEMINI_API_KEY` Edge Function secret already configured for `converse` — no new credential, no duplicated LLM wiring. New `career-guidance` Edge Function (`verify_jwt: true`, caller-JWT + service-role client pattern mirroring `book-mentor-session`): gathers only real, server-fetched context (the student's real resume content from the new Resume Builder tables, real active enrollments, and a real published-course catalog), asks Gemini for structured JSON (`responseSchema`-enforced: summary, skill gaps, recommended course IDs, action items), and **cross-checks every recommended course ID against the real catalog after generation — any ID the model invents is silently dropped, never shown** (concrete anti-hallucination guard, mirroring `converse`'s existing "grounded claim with zero sources" override). Results persist to a new `career_guidance_reports` table (owner-only SELECT RLS, no direct INSERT policy — written only by the function's service-role client) so students keep real history instead of re-generating (and re-spending API budget) on every visit. New `/dashboard/career-guidance` page (generate + history list), real sidebar nav entry. Live-verified the RLS: the owning student sees their report, a different student sees zero rows, and a direct client-side insert is rejected. **Not verified**: a full authenticated end-to-end invoke of the deployed Edge Function itself — no test-user session/credentials were available in this tool environment to sign in and call it from a real browser session; the function was code-reviewed and deployed successfully (version 2, ACTIVE) but a live click-through is still recommended before considering this fully production-validated.

**Phase 1 (Student Module) is now fully complete** — every item in the original scope (Resume Builder, AI Career Guidance, Mock Tests & Quiz System, Notes, Learning Analytics, Student Profile, Placement Module, remaining dashboard placeholders) is built, verified, and documented.

## Phase 2.0 — Design system foundation (2026-07-27)

Per explicit direction: before any page redesign, establish the complete design system foundation. Audited the existing system first (`docs/DESIGN_SYSTEM.md` already existed from the 2026-07-24 Admin Dashboard pass) rather than rebuilding it — extended it to close real gaps against the requested deliverable list, and produced the redesign roadmap. **No pages were redesigned in this pass** — foundation and planning only, per explicit instruction.

- [x] **Tokens**: added motion tokens (`--duration-fast/base/slow`, `--ease-standard/emphasized`) and a validated chart categorical palette (`--chart-1..8`, light+dark, run through the `dataviz` skill's `validate_palette.js` against this app's real card surfaces `#FFFFFF`/`#10192B` — both pass every hard gate).
- [x] **Component library gaps closed**: added `Avatar`, `Tooltip` (package was already installed with zero consumers), `Popover`, `Switch`, `Separator`, a shadcn-style `Form`/`FormField`/`FormItem`/etc. wrapper (`react-hook-form` was installed but only used ad-hoc in `AuthPage.tsx`), and a `Chart` wrapper (`ChartContainer`/`ChartTooltipContent`/`ChartLegendContent`) that drives `recharts` entirely off the new `--chart-N` tokens instead of each page inventing its own colors.
- [x] Extracted `Breadcrumbs` (previously inline in `AdminPage.tsx`) into `src/components/common/Breadcrumbs.tsx` with `rootPath`/`rootLabel` props, so the Mentor and Student shells can reuse it during their redesign steps instead of each writing their own.
- [x] **Page inventory**: audited and grouped every page (Student/Mentor/Admin/Marketing/Auth) in the new `docs/REDESIGN_ROADMAP.md`, with the prioritized redesign order per explicit direction (Auth → Dashboard Layout → Student Dashboard → Mentor Dashboard → Admin Dashboard → Course Pages → CRM → Analytics → Profile Pages → remaining pages).
- [x] Documented the reference-quality direction and explicit anti-patterns (no glassmorphism/decorative gradients/flashy animation outside the marketing hero) in `DESIGN_SYSTEM.md` § 1, and the two-motion-system boundary (Framer Motion for product surfaces, GSAP for marketing only) in § 6.
- [x] Full verification suite green (`tsc`/`eslint`/`vitest`/`build`); 2 new (accepted) ESLint warnings from `chart.tsx`/`form.tsx` co-locating a hook with components — the same convention upstream shadcn/ui ships with, not a code-quality regression.

## Phase 2.1 — Page-by-page redesign: Authentication + Dashboard Layout (2026-07-27)

Per explicit direction to redesign incrementally, only moving to the next page/group once the current one is production-ready. Full details in `docs/REDESIGN_ROADMAP.md`'s new "Progress" section — summary here:

- [x] **Authentication** (`AuthPage.tsx`, `ForcePasswordChangePage.tsx`, `VerifyEmailPage.tsx`, `student/ProfileCompletionPage.tsx`): migrated off hardcoded hex colors onto design tokens; added a missing `<h1>`; added real keyboard support (roving tabindex + arrow keys) to the Sign In/Create Account tab switcher; converted the signup role selector to native radio inputs for free ARIA APG compliance; added inline field-level errors with `aria-invalid`/`aria-describedby`/`role="alert"` where they were toast-only before; added a `--destructive-text` token after an independent accessibility review found the existing `--destructive` color fails 4.5:1 text contrast on white despite being fine for icons/borders. **Real bug found and fixed**: `ProfileCompletionPage` wrote education/skills into `profiles.metadata` (read by nothing) instead of `student_profiles` (read by Resume Builder/AI Career Guidance) — onboarding data was silently going nowhere.
- [x] **Dashboard Layout** (Student shell: `DashboardShell`/`DashboardSidebar`/`DashboardTopbar`; Admin shell: `AdminPage.tsx`): extracted a shared `useTheme()` hook and fixed a real persistence bug (dark mode never survived a hard refresh because nothing ever read `localStorage.theme` back on load - added an anti-FOUC init script in `index.html`). Wired two previously-disabled Student topbar buttons to real, already-existing features (dark mode toggle; chat quick-access) instead of leaving them permanently fake, and removed an unbacked "Streak/XP" decorative block entirely (no real system exists, matching the BUG-46 precedent). Added "Skip to content" links and fixed sidebar landmark/ARIA structure (single `<nav>`, `aria-current`, `aria-hidden` on decorative icons) on both shells. Mentor shell extraction deferred to the Mentor Dashboard step per the roadmap's own sequencing.
- [x] Independent accessibility review via the accessibility-lead agent on the Authentication group — findings applied (see above); full report in `docs/REDESIGN_ROADMAP.md`.
- [x] Full verification suite green (`tsc`/`eslint`/`vitest`/`build`) after every change, per the "only move to the next page after the current one is production-ready" instruction.

## Phase 2.2 — Page-by-page redesign: Student Dashboard (2026-07-27)

Per the same incremental discipline as Phase 2.1. Full details in `docs/REDESIGN_ROADMAP.md`'s "Progress" section — summary here:

- [x] **Student Dashboard** — every page in the Student group (`DashboardPage`, `MyCoursesPage`, `CourseLearningPage`, `MyCertificatesPage`/`CertificateViewPage`, `PaymentHistoryPage`, `ProfileSettingsPage`, `ChatPage`/`SupportPage`/`AssignmentsPage`, `StudentMentorsPage`, `WishlistPage`, `NotesPage`, `PlacementsPage`, `ResumeBuilderPage`, `CareerGuidancePage`) migrated off hardcoded hex/slate colors onto design tokens, with `focus-visible` rings, `aria-hidden`/`aria-label`/`aria-current`/`aria-expanded` added throughout. Print-output content (`ResumeBuilderPage`'s `#resume-preview`, `CertificateViewPage`) and video/PDF player chrome (`CourseLearningPage`) intentionally kept fixed colors regardless of theme, matching established convention.
- [x] **Real bugs found and fixed along the way**: three dead buttons with no click handlers (`MissionControlHero`'s "Resume" button, `TodaysFocus`'s "Start" button, `ContinueLearning`'s "Continue Learning" button) now wired to real course links, `MissionControlHero`'s unbacked "View Roadmap" button removed entirely; `MyCoursesPage` hardcoded 0% progress fixed by extending `useStudentCourses` to fetch real `course_progress`; `MyCoursesPage` thumbnails fixed from a broken hand-built URL against a private bucket to `resolveFileUrl()`; `PaymentHistoryPage`'s `alert()`-based "Download" button converted to an honestly-disabled state after confirming the backing Edge Function is undeployed and references a nonexistent column (pre-existing Phase 5 gap, not fixed further — out of scope for a redesign pass); `SupportPage`'s internal-only ticket messages were rendered `opacity-0` (invisible but taking up layout space) — now filtered out.
- [x] Full verification suite green (`tsc`/`eslint`/`vitest`/`build`) after every batch, per the "only move to the next page after the current one is production-ready" instruction.

## Phase 2.3 — Page-by-page redesign: Mentor Dashboard (2026-07-27)

Per the same incremental discipline as Phase 2.1/2.2. Full details in `docs/REDESIGN_ROADMAP.md`'s "Progress" section — summary here:

- [x] **Mentor Dashboard shell extraction** — pulled the inline header + tab-sidebar out of `MentorDashboardPage.tsx` into a reusable `MentorDashboardShell` component (`src/components/mentor/layout/`), mirroring the Student/Admin shell pattern. Added a skip-to-content link and a real dark-mode toggle wired to the shared `useTheme()` hook — the Mentor dashboard previously had no dark-mode toggle at all.
- [x] **Split the 8 dashboard tabs into 8 separate components** (`src/components/mentor/dashboard/`) — `MentorDashboardPage.tsx` dropped from 624 lines to a thin composition; no tab behavior changed, file organization only.
- [x] Migrated hand-rolled status-color spans onto the shared `Badge` component (course status, enrollment status, review-approval), analytics stat colors onto `text-success`/`text-warning` tokens, and loading states onto `text-muted-foreground` + `Skeleton`.
- [x] Accessibility fixes: `aria-hidden` on decorative icons throughout, `scope="col"` + scroll wrapper on the Students table, `role="tablist"`/`role="tab"`/`aria-selected` on the Profile sub-tab switcher, `aria-current="page"` on the active dashboard tab, proper `<label htmlFor>` associations on the profile edit form, `focus-visible` rings added where missing.
- [x] Full verification suite green (`tsc`/`eslint`/`vitest`/`build`) after the extraction.

## Phase 2.4 — Page-by-page redesign: Admin Dashboard (2026-07-27)

Per the same incremental discipline as Phase 2.1/2.2/2.3. Full details in `docs/REDESIGN_ROADMAP.md`'s "Progress" section — summary here:

- [x] **Closed the remaining `DataTable` migration gaps** noted in `docs/DESIGN_SYSTEM.md` § 8: `AdminAssignmentsPage`, `AdminAuditLogsPage`, and `AdminInvitationsPage` rebuilt onto the shared `DataTable` component with proper column defs and CSV export. `AdminCalendarPage`/`AdminChatPage` (not tabular) and `AdminSupportPage` (card list with a detail drawer) instead got the same token/Badge/accessibility pass.
- [x] Removed `GsapReveal` from every Admin page that had it — GSAP reveals are reserved for the marketing site per the design system's two-motion-system rule; dashboard surfaces don't use marketing-style entrance animation.
- [x] Replaced every hand-rolled status/priority/category color map with the shared `Badge` component across all 6 pages touched.
- [x] Accessibility fixes: `aria-hidden`/`aria-label` throughout, `<label htmlFor>` associations on previously-unassociated form inputs, `aria-current`/`aria-pressed` on active filters/selections, modal backdrops migrated to the `bg-foreground/40` token.
- [x] Full verification suite green (`tsc`/`eslint`/`vitest`/`build`); ESLint warnings dropped from 61 to 49 (incidental cleanup).

## Phase 2.5 — Page-by-page redesign: Course Pages (2026-07-27)

Per the same incremental discipline as Phase 2.1-2.4. Full details in `docs/REDESIGN_ROADMAP.md`'s "Progress" section — summary here:

- [x] **`AdminCoursesPage.tsx`**: migrated the slide-out course editor drawer (basic/curriculum/pricing/SEO/mentors/media tabs) onto design tokens — the list/`DataTable` view was already migrated 2026-07-24, but the drawer still had extensive hardcoded slate/cyan literals and `dark:` overrides. Converted status/action colors onto `Badge`, drawer tab switcher onto `role="tablist"`/`aria-selected`, all form fields onto `<label htmlFor>`.
- [x] **`CoursesPage.tsx`**: kept intentional marketing-page conventions (hex brand colors, `GsapReveal`) per the established `HomePage.tsx` precedent. **Real bugs fixed**: the counselling lead-capture modal faked success with zero backend call and unlabeled inputs — wired to the real `submitLead()` service (added email + consent fields to satisfy the real schema) matching `ContactPage.tsx`'s exact convention; the expandable category header was a non-keyboard-operable `<div onClick>` — converted to a real `<button aria-expanded>`.
- [x] **`CourseDetailsPage.tsx`**: **real bugs fixed**: hero thumbnail built from a broken hand-rolled URL against the wrong/private bucket (same class of bug already fixed in `MyCoursesPage`) — fixed via `resolveFileUrl()`; a dead "Play preview" button with no backing data removed; four fake `<span>` "share" icons with zero interactivity wired to a real native-share/clipboard handler plus real platform share-intent links.
- [x] Accessibility pass: `<nav aria-label>` on breadcrumb/tab nav, `aria-hidden` on decorative icons, `focus-visible` rings throughout, `aria-pressed` on the wishlist toggle.
- [x] Independent accessibility review via the accessibility-lead agent — findings applied: rebuilt both the `CoursesPage.tsx` counselling modal and the `AdminCoursesPage.tsx` editor drawer onto real `@radix-ui/react-dialog` primitives (neither hand-rolled dialog had a real focus trap, initial focus, or working Escape-to-close); replaced `AdminCoursesPage.tsx`'s hand-rolled row action menu with the real `DropdownMenu` primitives already used correctly by `DataTable.tsx` (the old version had no `role="menu"`, no arrow-key nav, and a tab-order bug); fixed a set of custom Tailwind classes (`slate-450`/`-350`/`-850`/`-150`) that don't exist in this project's scale and were silently compiling to no CSS in `CoursesPage.tsx`; added "(opens in a new tab)" to share-link accessible names.
- [x] Full verification suite green (`tsc`/`eslint`/`vitest`/`build`); ESLint warnings held at 49 (no regressions).

## Phase 2.6 — Whole-project lint/warning cleanup (2026-07-27)

Per explicit direction to bring the full verification suite to zero errors/warnings before starting any further feature work (not scoped to the redesign roadmap — every file in the repo was in scope this time).

- [x] Ran `tsc --noEmit`, `eslint .`, `vitest run`, `npm run build` to get a full baseline: `tsc` was already clean; `eslint` had 49 pre-existing warnings accumulated across earlier phases; tests/build were already green.
- [x] **Fixed all 49 ESLint warnings**, file by file:
  - `src/components/ui/chart.tsx`/`form.tsx`: added justified `eslint-disable-next-line react-refresh/only-export-components` comments — both are shadcn/ui primitives that ship a hook alongside their components by upstream convention; splitting them into separate files would deviate from the canonical drop-in structure for a purely cosmetic Fast Refresh optimization.
  - Removed 13 genuinely unused imports/types across `course.validator.ts`, `enrollment.mapper.ts`, `useAdminData.ts`, `AdminCouponsPage.tsx`, `AdminRefundsPage.tsx`, `courses.repository.ts`, `enrollments.repository.ts`, `course.service.ts`, `mentors.service.ts`.
  - `useProgress.ts`: prefixed a genuinely-unused destructured `courseId` param with `_`.
  - `AdminChatPage.tsx`: fixed a `useEffect` missing-dependency warning by depending on `[selectedConv]` instead of `[selectedConv?.id]`, matching the already-correct pattern in the student `ChatPage.tsx`.
  - `AdminMentorsPage.tsx`: removed the unnecessary `async` keyword from three handlers that only called `.mutate()` (fire-and-forget, no `await` needed); added a justified `eslint-disable-next-line react-hooks/exhaustive-deps` on a `useMemo` column-def array, since the flagged handlers don't close over anything that changes between renders (mentor is passed as a call argument, mutation objects are referentially stable) — adding them to deps would only force the whole column array to recompute every render for no behavioral benefit.
  - `admins.service.ts`: five intentional "disabled during schema sync" stub functions (`upsertAdmin`/`updateAdmin`/`assignAdminRole`/`disableAdmin`/`deleteAdmin`) were `async` functions that only `throw` (no `await`) with unused params — rewritten as plain (non-`async`) functions returning `Promise.reject(...)` explicitly, matching the file's own existing `createAdminUser` convention; this preserves identical Promise-rejection semantics for any future caller while satisfying both the unused-arg and no-await lint rules honestly rather than papering over them.
  - **Deleted confirmed-dead code** rather than lint-suppressing it: `src/repositories/admin.repository.ts`, `src/services/admin.service.ts`, and `src/hooks/admin/useAdminStudents.ts` formed a stub-only chain (`getStudents`/`updateStudentStatus` returned hardcoded empty data / an always-failing stub) with **zero real consumers anywhere in the app** — `AdminStudentsPage.tsx` uses the real `useStudents`/`useAdminData.ts` path from the BUG-29 fix instead. Verified via full-repo grep before deleting; confirmed no dangling imports afterward.
  - `DashboardPage.tsx`: `statsError`/`continueLearningError` were destructured from `useStudentDashboard` but never read anywhere — rather than silencing the warning, wired them to the page's existing `WidgetError`/`ErrorBoundary` fallback pattern so a real query-level error (not just a render-time exception, which is all `ErrorBoundary` catches) now actually surfaces to the student instead of being silently swallowed.
  - `scripts/test_learning_e2e.js`: removed a redundant `/* eslint-env node */` comment (the file already has a blanket `/* eslint-disable */` above it) that was triggering a Node.js flat-config deprecation warning on every lint run.
- [x] Re-ran the full verification suite after all fixes: `tsc --noEmit` clean, `eslint .` **0 errors, 0 warnings** (down from 49), `vitest run` 63/63 passing, `npm run build` succeeds (only the pre-existing, unrelated >500 kB chunk-size advisory from the Vite reporter remains — an informational note about code-splitting opportunity, not an error/warning from any of the four required tools, and unchanged all session).

## Enterprise Mentor Management — Phase 1: Account Ownership & Security Controls (2026-07-29)

Full audit run first (mentor pages/services/repositories/auth architecture/schema/audit logging/CRM tables/bulk-action hooks) before any code — see plan file for the complete inventory. Confirmed reusable: `mentors.service.ts` CRUD basics, `mentor_profiles`/`mentor_permissions`/`mentor_notes` tables, full CRM schema (`tasks`/`meetings`/`calendar_events`), `login_history`/`user_sessions` tables (existed but nothing populated them), `create-mentor`'s saga/rollback pattern. Confirmed genuinely missing: password override, forced logout, account lock independent of suspend, audited security actions.

- [x] Additive migration `20260729000001_mentor_account_security.sql`: `mentor_profiles` gains `login_disabled`/`locked_at`/`locked_by`/`locked_reason`; new RPCs `log_login_event()` (client calls after a successful sign-in) and `force_logout_user()` (admin-only, deletes the user's `auth.refresh_tokens` rows + closes open `user_sessions`).
- [x] New Edge Function `admin-mentor-account` (same validate-role → act → audit-log saga pattern as `create-mentor`): `set_password` (`auth.admin.updateUserById`, password never logged/returned), `force_password_change` (reuses existing `user_settings.password_reset_required`), `force_logout`, `lock`/`unlock`. Privileged RPCs are called through the **caller's own JWT-scoped client**, not the service-role client — `auth.uid()`-based checks (`force_logout_user`, `log_audit_event`) would silently no-op or fail their own admin check otherwise.
- [x] `IdentityService.loadCurrentUser()` now signs out and rejects with `AuthorizationError` if `mentor_profile.login_disabled` is true — enforced app-side on every identity load, not just at the admin action boundary.
- [x] **Found and fixed a real pre-existing bug during live verification**: `log_audit_event()` (defined in `20260718000005_enterprise_features.sql`) inserts into an `audit_logs.details` column that never actually existed on the live table — every call to this RPC had been silently failing with `42703` since it was written, undetected because nothing had ever called it until this pass. Fixed with an additive column migration (`20260729000002_audit_logs_details_column_fix.sql` equivalent — applied as `audit_logs_details_column_fix`).
- [x] Frontend: `mentors.service.ts` gained `setMentorPassword`/`forceMentorPasswordChange`/`forceMentorLogout`/`lockMentor`/`unlockMentor`/`listMentorLoginHistory`/`listMentorActiveSessions`; `useMentorMutations()` gained matching mutations; `AdminMentorsPage.tsx` gained a "Security" tab (set password, force logout, lock/unlock with reason, login history, active sessions) — replaced the old ad-hoc un-audited `forcePasswordReset()` helper that wrote directly to `user_settings` with the new audited edge-function call.
- [x] Live-verified end-to-end against production (disposable test admin + test mentor created via SQL, cleaned up after): all 5 edge function actions returned success; confirmed via direct SQL that `audit_logs` recorded old/new values correctly for lock/unlock/force-logout, `user_sessions.is_active` flipped to false with `ended_at` set, `user_settings.password_reset_required` flipped true, the new password actually worked for sign-in, `log_login_event` inserted a real `login_history` row, and `get_current_identity()` correctly surfaces `login_disabled` in the `mentor_profile` payload mentor-side.
- [x] Full verification suite: `tsc --noEmit` clean, `eslint .` 0 errors/0 warnings, `vitest run` 63/63 passing, `npm run build` succeeds.
- **Not done this pass** (Playwright MCP was disconnected this session): a live browser click-through of the new Security tab. Recommend one manual pass before considering this UI fully verified.

## Enterprise Mentor Management — Phase 2: CRM, Task/Meeting Management, Course Ownership, Performance (2026-07-29)

Reused everything the audit already confirmed existed: `mentor_notes` table (unwired, now wired), `crmService.listTasks/createTask/updateTask` (already generic via `entity_type`/`entity_id` — no new task service needed, just called with `entity_type: 'mentor'`), `task_comments` table (unwired, now wired), `meetings` table + `mentor-booking.service.ts`'s existing host/attendee pattern, `auditLogs.service.ts` (extended with an `entityId` filter for a per-mentor timeline), `notificationsService.sendNotification()` (reused as-is for task/meeting notifications).

- [x] `crm.service.ts` extended: `getMentorNotes`/`addMentorNote` (mirrors the existing `getStudentNotes`/`addStudentNote` pattern exactly), `getTaskComments`/`addTaskComment`, and a `MENTOR_TASK_TYPES` preset list (Review Assignment/Resume/Project, Call Student, Interview Student, Update Course, Publish Lesson, Upload Notes, Verify Certificate, Conduct Live Session, Custom Task) — presets pre-fill the existing free-text `tasks.title`, no schema change. Priority "Critical" maps onto the existing `urgent` DB value (no new enum value added).
- [x] `mentor-booking.service.ts` extended: `listMentorMeetings` (full history, host OR attendee), `createAdminMeeting` (mentor/student/internal/interview types via the existing `entity_type` column), `completeMeetingWithOutcome` (outcome recorded in the existing `notes` column — no separate outcome column exists).
- [x] `mentors.service.ts` extended: `listMentorCourses` (enrollment count, rating, avg completion per course), `reassignMentorCourses` (bulk `courses.mentor_id` update + `log_audit_event` per course — course ownership transfer effectively transfers all its enrolled students at once), `getMentorPerformance` (students assigned, courses, assignments reviewed via `assignment_submissions.graded_by`, quiz attempts, certificates issued, avg rating, avg completion, upcoming meetings, pending/completed tasks, avg task turnaround time).
- [x] New `src/hooks/admin/useMentorCRM.ts` and `src/components/admin/mentors/MentorCRMPanel.tsx` — three new tabs on the existing `AdminMentorsPage.tsx` drawer: **CRM** (Notes/Tasks/Meetings/Timeline sub-tabs), **Courses** (ownership + bulk transfer to another mentor), **Performance** (11-metric dashboard).
- [x] Live-verified against production (disposable admin/mentor/mentor2/student/course fixtures via SQL, cleaned up after): confirmed via direct SQL that a mentor note, a task, a task comment, a meeting, and a full course-ownership transfer (with its `course_reassigned` audit log entry) all wrote correctly through real RLS.
- [x] Full verification suite: `tsc --noEmit` clean, `eslint .` 0 errors/0 warnings, `vitest run` 63/63 passing, `npm run build` succeeds.
- **Deferred / scope decisions made explicitly rather than guessed**:
  - **Per-student mentor reassignment independent of course ownership**: no such table exists (enrollments have no `assigned_mentor_id`); building one would fork from the course-based "mentor's students" model every other surface in the app already relies on (mentor dashboard, StudentsTab, RLS policies). Treated as a genuine business-policy decision, not made unilaterally — course-level transfer (which does move all its students) ships now; a dedicated per-student assignment layer is a follow-up if the business actually needs partial-course reassignment.
  - **Call Log**: no dedicated table exists; mapped onto `tasks` with type "Call Student" rather than inventing a new table.
  - **Attendance metric**: no attendance-tracking data source exists anywhere in the schema; left out of the Performance dashboard rather than fabricated.
  - **Response Time**: implemented as "Avg. Task Turnaround" (`completed_at - created_at` on the mentor's tasks) — a real, derivable metric — rather than a generic unimplementable "response time."
  - Task/meeting **attachments**: no generic attachment mechanism exists for either table yet; deferred to Phase 3 (documents).
- **Not done this pass** (Playwright MCP disconnected): live browser click-through of the new CRM/Courses/Performance tabs.

## Enterprise Mentor Management — Phase 3: Documents, Bulk Actions, Advanced Export (2026-07-29)

- [x] New `mentor_documents` table (additive migration `20260729000003_mentor_documents.sql`) with real version history (each upload marks the prior version `is_current=false` and inserts a new row, never overwrites/deletes). Reuses the existing private `admins` storage bucket (already admin-only via its unconditional RLS policy) and the existing `files` table for storage metadata — no new bucket, no new env var. Document types: resume, certificate, agreement, identity, portfolio, other. New "Documents" tab on `AdminMentorsPage.tsx` (upload, view/download via signed URL, delete, expandable version history per type).
- [x] Extended `storage.service.ts`'s shared `allowedMimeTypes` to include `.doc`/`.docx` (resumes/agreements commonly arrive as Word docs, not just PDF) — a shared change, so this benefits every upload surface in the app, not just mentor documents.
- [x] Bulk actions on `AdminMentorsPage.tsx`'s mentor list: Activate, Suspend, Restore, Delete, Notify (compose-and-send modal) via the DataTable's existing `bulkActions`/row-selection support (same pattern as `useAdminCourses.ts`'s `useBulkUpdateCourseStatus`/`useBulkDeleteCourses`). Bulk Course Reassignment is a separate modal (searchable course picker + destination-mentor select) rather than a per-row action, since `courses.mentor_id` is a single required owner — "bulk" only makes sense as "many courses → one mentor," not "many mentors → one course."
- [x] **Found and fixed a second real, load-bearing bug during live verification**: `notifications` had no admin SELECT policy — only "view own" (`recipient_id = auth.uid()`). Postgres RLS requires an `INSERT ... RETURNING` to also satisfy the table's SELECT policy for the row being returned, and `notificationsService.sendNotification()` (used everywhere: task assignment, meeting scheduling, bulk notify, broadcast) always does `.insert().select().single()`. Since the recipient is virtually never the admin themselves, **every admin-initiated notification to someone else had been silently failing with 42501 since this function was written** — this was never caught before because nothing had live-tested it end-to-end (via curl against production) until this pass. Fixed via `20260729000004_notifications_admin_select_fix.sql` (additive: admins can already write any notification; now they can also read any notification, which `RETURNING` needs).
- [x] Advanced export: added `exportToExcel`/`exportToPDF` to `src/utils/export.ts` (alongside the existing `exportToCSV`), both lazy-loaded via dynamic `import()` so they don't bloat the main bundle. Export rows extended to include Status, Locked, and Last Login (batched single query across the visible page, not N+1).
  - Dependency note: the npm registry's `xlsx` package is deliberately frozen on old versions with known CVEs (prototype pollution `GHSA-4r6h-8v6p-xvw6`, ReDoS `GHSA-5pgg-2g8v-p4x9`) — SheetJS only publishes patched builds via their own CDN. Installed `xlsx@0.20.3` directly from `https://cdn.sheetjs.com/xlsx-0.20.3/xlsx-0.20.3.tgz` (the officially documented SheetJS install method) instead of the vulnerable registry package. Tried `exceljs` first as an alternative — rejected because it transitively pulls `archiver`/`glob`/`zip-stream` with their own high-severity CVEs and no non-breaking fix. `npm audit` after all three attempts: 7 vulnerabilities, all pre-existing/unrelated dev-tooling transitives (`brace-expansion`, `form-data`, `js-yaml`, `postcss`, `react-router`, `vite`), none newly introduced.
- [x] Full verification suite: `tsc --noEmit` clean, `eslint .` 0 errors/0 warnings, `vitest run` 63/63 passing, `npm run build` succeeds (new xlsx/jspdf chunks confirmed lazy-loaded, not in the main bundle).
- [x] Live-verified against production: full document upload → version 2 → old-version signed-URL retrieval flow; bulk-parallel status updates; the notifications RLS bug reproduction and fix, confirmed via a real insert-with-representation call before and after the migration. All test fixtures cleaned up after.
- **Next**: public mentor pages audit (`MentorsPage.tsx`/mentor profile page), accessibility review, and a live browser pass — in progress, see below once complete.

## Enterprise Mentor Management — Phase 3 continued: Public Pages, Accessibility, Live Browser Verification (2026-07-29)

- [x] **Public `MentorsPage.tsx` rewritten to real data** — was rendering a hardcoded 4-mentor array from `src/data/platform.ts` with two dead buttons ("How Mentorship Works", "View All Mentors"), a decorative single-option sort `<select>`, fake pseudo-tabs with no real content switch, and a hardcoded fabricated testimonial quote. Replaced with a real `mentorRepository.listCatalog()` query (new method, joins real `mentor_profiles`/`profiles`/`courses`/`enrollments`/`testimonials`), a working 3-option real sort, category tabs derived from actual mentor expertise data, and removed the fabricated content entirely rather than papering over it.
- [x] **Accessibility fixes across the new Phase 1-3 admin UI and public mentor pages**, per a dedicated accessibility-lead review: all 4 overlays in `AdminMentorsPage.tsx` (editor drawer, credentials modal, bulk-notify modal, bulk-reassign modal) converted from bare `div`+`AnimatePresence` to the project's own established `@radix-ui/react-dialog` pattern (matching `AdminCoursesPage.tsx`) — real focus trap, Escape-to-close, `aria-modal`, focus return; both tab bars (8-tab drawer nav, 4-tab CRM sub-nav) converted to proper `role="tablist"`/`role="tab"`/`aria-selected`; every icon-only button given a real `aria-label` (unique per document version in the Documents panel, not a repeated generic label); every previously-unlabeled input/select given a label; `aria-hidden="true"` added to decorative icons throughout; `MentorBookingModal.tsx` gained `role="dialog"`/`aria-modal`/Escape-to-close/focus-on-open; `MentorProfileHeader.tsx`'s breadcrumb is now real clickable `Link`s instead of static text; `MentorFAQ.tsx` accordion triggers gained `aria-expanded`/`aria-controls`; `MentorCourses.tsx`'s course link changed from a raw `<a>` (full page reload) to a real `Link`.
- [x] **Found and fixed BUG-46 (CRITICAL) via a genuinely logged-out live browser session**: the public mentor profile page has been unreadable by any real anonymous visitor since Phase 1.19 — `profiles` RLS never had a public SELECT policy, and every public mentor query joined `profiles` directly. Fixed with a narrow `get_public_mentor_profiles` RPC (not a blanket RLS widening, which would leak PII). Also found and fixed BUG-47 (student-count stats silently read 0 for anon visitors — `enrollments` correctly has no public policy) and BUG-48 (`get_mentor_available_slots` had no `anon` execute grant, so the booking widget 401'd for every logged-out visitor).
- [x] **Live browser verification performed for real** (Playwright, `localhost:5173` against production Supabase) once the Playwright MCP reconnected: signed in as a disposable admin, clicked through every one of the 8 mentor-editor tabs (Security, CRM→Notes/Tasks/Meetings/Timeline, Courses, Performance, Documents) with real writes — set a password (confirmed via toast), added a CRM note (confirmed it rendered), confirmed the Timeline tab showed the real audit-log entry from the password-set action, uploaded a real file to the Documents tab and confirmed the exact `aria-label`s from the accessibility fixes rendered correctly, then deleted it. Separately signed all the way out and reloaded `/mentors` and `/mentors/:slug` as a genuine anonymous visitor — confirmed real mentor names/data render and zero console errors (this is what caught BUG-46/47/48). All disposable test users/files/storage objects cleaned up afterward.
- [x] Full verification suite re-run clean after all fixes: `tsc --noEmit`, `eslint .` (0/0), `vitest run` (63/63), `npm run build`.

**Enterprise Mentor Management is now feature-complete across all 3 phases** (Account Security, CRM & Operations, Documents/Bulk/Export + public-page parity), with every capability live-verified against production — not just linted — including a real signed-out browser session, which is what surfaced the three most severe bugs (BUG-46/47/48) this entire initiative found.
