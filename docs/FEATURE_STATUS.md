# SkillGuru — Feature Status

_Last analyzed: 2026-07-24 (Phase 1.10 QA pass). Verdicts: **Complete** (production-ready, real data, no obvious stubs) / **Partial** (real for the core path, with specific stubbed sub-parts called out) / **Stub** (UI exists but data layer is fake/hardcoded/throws) / **Unclear** (not verified deeply enough in this pass to state confidently). Entries updated 2026-07-24 reflect Phase 1.4–1.9 work and are marked accordingly; everything else is carried over from the 2026-07-23 pass and may be stale._

## Authentication & Onboarding

| Feature | Status | Notes |
|---|---|---|
| Login / Signup (student) | **Complete** | `AuthPage.tsx`, full validation, cooldown/rate-limit UX, role selection. See AUTH_FLOW.md. |
| Email verification | **Complete** | `WAITING_EMAIL_CONFIRMATION` state, `/verify-email` page, resend flow. |
| Password reset | **Complete** | `authService.resetPassword`, mapped errors. |
| Profile completion enforcement | **Complete** | `ProtectedRoute` force-redirects incomplete student profiles to `/onboarding`. |
| Session persistence / token refresh | **Complete** | Delegated to Supabase client (`persistSession`, `autoRefreshToken`). |
| Role-change propagation to open sessions | **Stub-like gap** | No refetch mechanism; requires reload/re-login. See ROLE_FLOW.md § 4. |

## Admin — Dashboard & Analytics

| Feature | Status | Notes |
|---|---|---|
| Admin Dashboard landing page | **Complete** _(2026-07-24)_ | All 8 previously-hardcoded widgets rewired to real data (`useDashboardData()`, `useMentors()`, `analyticsService.getTopCourses()`, direct real queries); `QuickActionsWidget` was already real. Verified in a live logged-in browser session, 0 console errors. |
| Admin Analytics/Reports page | **Complete** _(2026-07-24)_ | `AdminAnalyticsPage.tsx` confirmed already fully real (revenue/enrollment/student-growth/CRM-funnel, honest empty states) — the one fabricated figure it depended on (`getTopCourses()` revenue approximation) has been fixed. |
| `dashboard.service.ts` backing service | **Complete** _(2026-07-24)_ | Real revenue/leads/orders/certificates; fabricated `placementRate` removed entirely (no real data source exists). Now wired to the dashboard page via `useDashboardData()`. |
| Audit logs (dedicated page) | **Complete** | `AdminAuditLogsPage.tsx` — real inline query, CSV export. |
| Audit logs (elsewhere: Settings tab, Role page) | **Partially fixed** _(2026-07-24)_ | Was three code paths for "show audit logs," two fake: `useAuditLogs` in both `useAdminSystem.ts` and `useAdminData.ts`/`auditLogs.service.ts` hardcoded `[]`. Both stubs and the dead `auditLogs.service.ts` deleted; `AdminSettingsPage`'s Audit tab now honestly links to the one real implementation (`AdminAuditLogsPage.tsx`) instead of duplicating it. See BUG-34. **Still open**: `AdminRolePage.tsx` still uses a hardcoded `mockAuditLogs` array (BUG-14) — not touched this pass, out of scope for the Notifications/Settings migration. |

## Admin — Course / Student / Mentor Management

| Feature | Status | Notes |
|---|---|---|
| Course CRUD (`AdminCoursesPage`) | **Complete** _(2026-07-24)_ | Was **mostly fake inside the editor drawer** (this earlier "Mostly complete" verdict undersold it) — Duration/Price were inert `value={0}` inputs, Curriculum showed 3 hardcoded fake modules with a no-op "Add Module" button, Mentors showed 4 hardcoded fake names as disabled checkboxes, Media had a fake upload toast, SEO was fully decorative, and the Enrollments column was a hardcoded `0`. All rebuilt against real data this pass — see BUG-31. SEO tab is honestly disabled (no schema support), matching the existing Discount Subtitle pattern. |
| Student management (`AdminStudentsPage`) | **Complete** _(2026-07-24)_ | Was previously **100% fabricated** — a hardcoded fictional roster and fake certificate-approval/magic-link actions (this earlier "Mostly complete" verdict was wrong). Rebuilt against real `useStudents()`/enrollment/certificate data — see BUG-29. A separate, entirely dead stub chain (`admin.repository.ts`/`admin.service.ts`/`useAdminStudents.ts`, zero real consumers) that predated this rebuild was found and deleted 2026-07-27 during a lint cleanup pass — see BUG-51. |
| Mentor management (`AdminMentorsPage`) | **Enterprise-complete, all 3 phases** _(2026-07-29)_ | Real `listMentors`/`create-mentor` Edge Function flow, real rating, Suspend/Reactivate/soft-Delete/Restore (BUG-06). **Phase 1 (Security)**: set password directly, force logout everywhere, lock/unlock independent of suspend — via `admin-mentor-account` Edge Function, all audited. **Phase 2 (CRM & Operations)**: mentor notes, tasks, meetings, real activity timeline, course-ownership transfer, 11-metric performance dashboard. **Phase 3 (Documents/Bulk/Export)**: mentor document management with real version history, bulk activate/suspend/restore/delete/notify, bulk course reassignment, CSV/Excel/PDF export. Every capability live-verified in a real browser against production (not just linted) — including a genuinely signed-out session, which surfaced and led to fixing 3 critical/high pre-existing bugs (BUG-46/47/48: the public mentor pages were unreadable by real anonymous visitors). Deferred: per-student mentor reassignment independent of course ownership (genuine business-policy decision). |
| Public mentor catalog (`MentorsPage.tsx`) | **Complete** _(2026-07-29)_ | Was **100% static marketing content** (hardcoded 4-mentor array from `src/data/platform.ts`, two dead buttons, a fabricated testimonial quote) — see the old entry below, now superseded. Rebuilt against real `mentor_profiles`/`profiles`/`courses`/`enrollments`/`testimonials` data via a new `mentorRepository.listCatalog()` method; live-verified as a genuinely signed-out visitor. |
| Admin account management (`useAdmins`/`admins.service.ts`) | **Stub** | Every method either returns an empty page or throws "disabled during schema sync" / "not fully implemented." |
| Role/permission assignment UI (`AdminRolePage`) | **Partial** | Page renders; underlying `role_permissions` table has 0 rows in the DB, so no permission is actually enforceable via this system yet regardless of UI state (see ROLE_FLOW.md). |

## Admin — CRM

| Feature | Status | Notes |
|---|---|---|
| Leads CRUD, notes, followups, tasks, pipeline/Kanban, CRM metrics | **Complete** | `crm.service.ts` is the most mature service in the codebase — real queries, soft deletes, consistent error handling. |
| Public lead-capture forms (Contact/Enquiry/Counselling/Demo) | **Broken** | Frontend (`src/services/leads.ts`) calls the `leads` Edge Function, which inserts columns (`full_name`, `source`, `course_slug`, `message`) that do not exist on the live `leads` table (real columns: `name`, `source_id`, `course_interest_id`, `budget`, etc.). This will fail at runtime — see BUG_REPORT.md. |
| Fake duplicate leads hooks | **Stub (shadow copy)** | `useAdminLeads.ts` defines an entirely hardcoded-empty parallel implementation of `useAdminLeads`/`useLeadMutations`/bulk-update hooks, separate from the real ones in `useAdminData.ts`. Whichever gets imported by a given component determines whether that screen actually works. |

## Admin — Commerce / Payments

| Feature | Status | Notes |
|---|---|---|
| Checkout (Mock/Razorpay order creation) | **Complete (Mock provider)** _(2026-07-24)_ | `PaymentProvider` abstraction built (`MockPaymentProvider`/`RazorpayProvider`); `create-order`/`verify-payment` rewritten against the real schema and deployed. Client-side insecure dev-fallback removed entirely. `RazorpayProvider` is written but untestable until real credentials exist (Phase 1.11). |
| Payment verification (`verify-payment` Edge Function) | **Complete (Mock provider)** _(2026-07-24)_ | Deployed, real schema, idempotent, frontend calls it by its real name via `paymentService`. |
| Payment webhook (Razorpay → Supabase) | **Complete (Mock provider)** _(2026-07-24)_ | Deployed with HMAC signature verification (Mock or Razorpay secret); logs raw payload to `webhooks` before processing; live-tested with a real signed request. |
| Refunds (admin-triggered) | **Complete (Mock provider)** _(2026-07-24)_ | `refund-payment` now delegates to the active `PaymentProvider`; no longer hardcoded-mock regardless of configuration. |
| Invoice generation/download | **Partial** | `invoice-download` assembles real invoice data but returns JSON, not a PDF (comment: "Dummy PDF generation... real app would use pdf-lib"); `usePayment.ts`'s `useInvoice` hook is separately hardcoded to a fake `{amount:0, status:'paid'}` object regardless of the real function — and, found 2026-07-27, `PaymentSuccessPage.tsx` reads a `.invoice.*` shape the stub never actually provides, so the invoice-summary block silently never renders at all (see BUG-50). Needs a real `orders`/`payments` join before this can be wired for real — no `amount`/`status` column exists on `invoices` itself. |
| Admin Payments page (`AdminPaymentsPage.tsx`) | **Complete** _(2026-07-24)_ | Audited for the BUG-29-style fake-data pattern and found already fully real — `getAdminPayments`/`getRevenueMetrics`/`initiateRefund` all hit real tables/Edge Functions, no stubs. Migrated the hand-rolled `<table>` onto the shared `DataTable` component (built-in CSV export) for design-system consistency. See BUG-33. |
| Admin Certificates page (`AdminCertificatesPage.tsx`) | **Complete** _(2026-07-24)_ | Also already fully real (`fetchCertificates`/`backfillMissingCertificates` are real inline queries). Migrated onto `DataTable`; the "Verification" column previously showed a tautological "Issued" badge for every row — now links to the real public verification page with the actual verification code. See BUG-33. |
| Coupons, refunds admin pages | **Unclear** | Not deeply verified this pass. |
| Cart / Wishlist (`commerce.service.ts`) | **Unclear** | Service layer looks real (real CRUD against `cart`/`wishlists`), but no confirmed frontend page wiring was found in this pass — needs a dedicated check. |

## Admin — CMS / Website Content

| Feature | Status | Notes |
|---|---|---|
| System settings (`AdminSettingsPage` / `AdminCMSPage`) | **Complete** _(2026-07-24)_ | Was a full no-op stub — `getSetting` always returned `null`, `setSetting` did nothing, with a comment claiming "`system_settings` table is missing" (false — the table exists). Worse, `AdminSettingsPage`'s own "System Status" card explicitly claimed edits "will persist" while silently discarding them. Rewrote `cms.service.ts` for real against the actual key-value `system_settings` table; both `AdminSettingsPage` and `AdminCMSPage` use the same hooks, so both are now genuinely wired. See BUG-34. |
| Success stories | **Complete** _(2026-07-24)_ | Was a full stub — `successStories.service.ts` threw "table removed" on every write despite the real `success_stories` table existing with exactly the stub's own declared shape. Rewritten for real; the admin page's mutations also had no `onError` handler, so failures were silently swallowed — fixed. Stat tiles (top/average package) now computed from real data instead of hardcoded. See BUG-35. |
| Placements page | **Complete** _(2026-07-27)_ | Was an honest "Feature Not Available" stub (no schema existed). Fully rebuilt as an enterprise Placement Management System: Hiring Partners CRUD, Job Postings CRUD, and a full Applications pipeline (shortlist → interview rounds → HR round → selected → offer released → joined, or rejected/withdrawn at any point) with a real applicant drawer (schedule interviews, record feedback, release offers, mark joined) and a real status-history audit trail. See TASKS.md's Placement Module entry and DATABASE.md § 6f. |
| AI Guidance page | **Stub, explicitly labeled** _(2026-07-24)_ | Was previously a fully-fabricated live dashboard (hardcoded `mockAssessments` shown as real data, a fake webhook toggle, a fake "Test Event" round-trip, a pulsing "AI Engines Active" indicator claiming live automation) — this earlier "Stub" verdict undersold how misleading it was. Converted to an honest "Feature Not Available" placeholder matching `AdminPlacementsPage`'s pattern; no assessments table or n8n integration exists to wire it to. See BUG-36. |
| CMS Blogs tab | **Stub, explicitly labeled** _(2026-07-24)_ | Previously showed a hardcoded article list with a fully-interactive add/edit form whose "Save" only updated local React state ("(Local Only)" toast) — no `blog_posts` table exists. Removed entirely and replaced with an honest "Feature Not Available" placeholder. See BUG-37. |

## Mentor Portal

| Feature | Status | Notes |
|---|---|---|
| Mentor dashboard overview / assigned courses / students / analytics / profile tabs | **Complete** _(2026-07-24)_ | Wired to `mentor-portal.service.ts` / `useMentorPortal.ts`, real aggregation queries. Fixed the hardcoded `analyticsViews` metric (replaced with a real completed-enrollments count), a dead "Go to Classroom" button (now links to the real published course page), and the Profile tab (previously showed only a raw ID with a non-functional "Edit" button — now shows real name/email/headline/bio with a working save). See BUG-39. |
| Course builder (within Mentor Dashboard) | **Complete** _(2026-07-24)_ | Real module/lesson CRUD, reorder, video/PDF upload, Publish/Unpublish — built on the new `curriculum.service.ts`. |
| Notifications tab (Mentor Dashboard) | **Complete** _(2026-07-24)_ | Wired to the real `notifications.service.ts` via `useNotifications()`. |
| Student progress column (Mentor Dashboard) | **Complete** _(2026-07-24)_ | Reads real `course_progress.completion_percentage`. |
| Mentor invite flow | **Complete** _(2026-07-24)_ | Was broken end-to-end — `mentor-invite.service.ts` invoked a nonexistent Edge Function (`create-mentor-account`), silently caught the failure, and left every invite as a dead `pending` row with no auth account ever created. Fixed to call the real, deployed `create-mentor` function with its actual schema/response shape. `resendInvite` now branches correctly: forces a real password reset if the account already exists, or genuinely retries provisioning if the first attempt never succeeded. See BUG-38. |
| Mentor Dashboard Profile tab — Experience/Projects/Certifications/Achievements/Availability/Upcoming Sessions | **Complete** _(2026-07-27)_ | New sub-tabs, all genuine add/edit/delete(+reorder) CRUD against new real tables, real image upload for project/certification images, real weekly-availability management, real upcoming-sessions list with cancel. See BUG-41/TASKS.md Phase 1.19. |
| Mentor Dashboard Reviews tab (reply to reviews) | **Complete** _(2026-07-27)_ | New tab — real list of a mentor's course reviews (`testimonials.service.ts`) with genuine reply/edit-reply, backed by a SECURITY DEFINER RPC that verifies course ownership server-side. Reply also displays on the public profile. See TASKS.md Phase 1.20. |

## Student Experience

| Feature | Status | Notes |
|---|---|---|
| Student dashboard | **Mostly real** _(2026-07-27)_ | Wired to `useStudentDashboard()`. "Continue Learning" now genuinely resumes at the student's real last-incomplete lesson (was hardcoded to always the course's first lesson — see BUG-44), with a real mentor name and real module-progress counts. Sidebar's fabricated "Level 12/Storage/Daily Goal" panel replaced with real enrolled-course/certificate counts (see BUG-46). placementScore/streak/xp remain honest zeros (no backing feature exists — not fabricated, just genuinely absent), and a "Mentor Workspace & Calendar Integration" panel is explicitly disabled/labeled "Future Release" — honestly flagged in the UI itself. A real query-level failure on the stats or continue-learning data previously produced no visible feedback at all (the errors were fetched but never read) — now surfaces the page's existing `WidgetError` fallback per affected widget instead (see BUG-52). |
| Course browsing / course details | **Mostly real** _(2026-07-27)_ | Real course/curriculum data; mentor card, related courses, projects/skills/outcomes fall back to static marketing content (`data/platform.ts`) when DB data is missing. **Wishlist is now real** (was a disabled "Coming Soon" button in front of a fully-working, zero-consumer backend — see BUG-45): real add/remove with saved state, plus a new `/dashboard/wishlist` page. |
| Quiz / mock-test lessons | **Complete** _(2026-07-27)_ | Was entirely unbuilt on both sides (mentor authoring, student taking) despite a real schema and a `"quiz"` lesson content-type already existing — see BUG-47. Now: real mentor question/option authoring (MCQ, true/false) in the Course Builder, real student-side quiz-taking with server-side scoring (`submit_quiz_attempt` RPC) so the answer key never reaches the client. Fixed a real answer-key RLS leak found along the way. |
| Student Profile (`ProfileSettingsPage`) | **Complete** _(2026-07-27)_ | Name/phone edits were already real. Avatar upload was completely broken (uploaded to a storage bucket, `avatars`, that doesn't exist in this project) and the display URL used a file's database ID as if it were a storage path — see BUG-48. Also fixed a false "success" toast that fired before a profile save even ran, with no error handling at all. |
| Learning Analytics (student dashboard) | **Complete** _(2026-07-27)_ | The weekly study-hours chart was a permanent honest "coming in a future release" placeholder with a decorative, non-functional range dropdown. Real per-lesson time-tracking data (`lesson_progress.time_spent_seconds`) already existed and was going unused — built a real weekly/monthly study-hours aggregation (`studentService.getWeeklyStudyHours`) and made the range dropdown genuinely functional (This Week / This Month; "Last Week" dropped rather than left as a non-functional third option). Active-course/completion/certificate metrics were already real. |
| Course learning / lesson navigation | **Complete** _(2026-07-27)_ | Real curriculum navigation; lesson-completion tracking uses the correct per-lesson `progressService`. **Video/PDF player is now real** (was a literal `<p>Video Player (Mock)</p>` placeholder) — see BUG-42/43: fixed a storage-RLS folder-path bug that silently blocked every enrolled student from ever reading lesson media, then wired a real `<video>`/embedded-PDF viewer with real loading/error states and a real resources list. |
| Notes | **Complete** _(2026-07-27)_ | New feature — previously no table, service, or UI existed at all for a student's own per-lesson notes. New `lesson_notes` table (owner-only RLS, one note per student/lesson, soft-delete), an inline notes panel on the lesson-learning view, and a dedicated `/dashboard/notes` page listing all notes across courses with delete. Real sidebar nav entry. Live-verified owner-only RLS (non-owner reads zero rows, cannot forge an insert). |
| Placement | **Complete** _(2026-07-27)_ | New enterprise feature — previously a `disabled: true` sidebar tab in front of zero schema. Real browse/search/filter/save/apply flow (resume uploaded via the canonical `storage.service.ts` pipeline, then a real `apply_to_job` RPC — no fake success path), full application tracking with a status timeline and withdraw, upcoming interviews (reusing the same `meetings` row the mentor-booking flow already surfaces to attendees), and offer/placement history. See DATABASE.md § 6f. |
| Resume Builder | **Complete** _(2026-07-27)_ | New feature — previously no table, service, or UI existed at all. Reuses `profiles`/`student_profiles` for contact/summary/education/skills (their first real edit UI) and adds new owner-scoped tables for experience/projects/certifications/achievements. Real Edit + Preview UI at `/dashboard/resume-builder`; PDF export via the browser's native print-to-PDF, no new dependency. See DATABASE.md § 6g. |
| AI Career Guidance | **Complete, pending live E2E click-through** _(2026-07-27)_ | New feature — previously unbuilt for students. Reuses the AI Voice Agent's `AIProvider`/`GeminiProvider` abstraction and existing `GEMINI_API_KEY` secret (no new credential). New `career-guidance` Edge Function grounds every output in real resume/enrollment/course data and cross-checks recommended course IDs against the real catalog post-generation, dropping any invented ID. New `/dashboard/career-guidance` page. RLS live-verified; the deployed Edge Function itself has not yet been exercised end-to-end from a real authenticated browser session (no test credentials available in this pass). |
| Certificates | **Complete** _(2026-07-24)_ | Auto-issued on course completion (DB trigger), real student list/view/print, public verification page. Live-verified in a real logged-in browser session. |
| Payment history | **Complete (Mock provider)** _(2026-07-24)_ | Now backed by the real, working Mock payment pipeline. |
| Chat, Support, Assignments, Mentors listing (student) | **Complete** _(2026-07-27)_ | Audited and confirmed real: `chat_messages`/`conversations` (with a working Realtime subscription), `support_tickets`/`ticket_messages`, `assignments`/`assignment_submissions` (students can genuinely submit), and `mentor_profiles` (not the static marketing catalog) respectively. No fake-success paths found. |

## Notifications

| Feature | Status | Notes |
|---|---|---|
| Notification list/unread count/mark-read/send/broadcast/preferences/realtime | **Complete** | `notifications.service.ts` is one of the best-built services in the codebase, including a working Supabase Realtime subscription. |
| Student/mentor-facing notification bell | **Complete** _(2026-07-24)_ | Was a literal disabled placeholder button behind a 100%-stubbed shadow-pair service (`notification.service.ts`/`notifications.repository.ts`, both deleted). Now a real dropdown with live unread badge, mark-read, mark-all-read, realtime updates — live-verified in a real logged-in browser session. Notification preferences UI still not built. |

## Public Marketing Site

| Feature | Status | Notes |
|---|---|---|
| Home, Courses, About, Placements, Guidance, Contact pages | **Unclear (not audited this pass)** | This analysis focused on authenticated dashboards and the data layer; the marketing site's content/CMS wiring was not independently verified. |
| Mentors listing page (`MentorsPage.tsx`) | **Superseded — see "Public mentor catalog" row above** | Was static marketing content through 2026-07-27; rebuilt against real data 2026-07-29. |
| Mentor profile page (`MentorProfilePage.tsx`) | **Complete** _(2026-07-27)_ | Was ~90% fabricated (hardcoded education/certifications/projects/experience, fake reviews/testimonials attributed to invented named students) behind a real-looking DB-backed shell — see BUG-41. Fully rebuilt: real experience/projects/certifications/achievements (new normalized tables, mentor-managed via the Mentor Dashboard), real reviews (`testimonials` joined through `courses.mentor_id`), real courses-taught with real enrollment counts, real availability, and a real persistent booking flow (`get_mentor_available_slots` RPC + `book-mentor-session` Edge Function, mirroring the `enroll-free` server-validation pattern) with a real mentor notification on booking. `education` dropped (no UI consumer existed); `mentorshipProcess`/FAQ kept as generic non-personalized copy (not part of the fabrication problem, out of proportion to rebuild per-mentor). |

---

**Read this alongside BUG_REPORT.md and TECHNICAL_DEBT.md** — many "Stub" verdicts above are duplicated by a working real implementation elsewhere in the codebase under the same or a very similar name (the "shadow copy" pattern), which is itself the single most actionable piece of technical debt in this project.
